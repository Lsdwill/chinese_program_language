# 华言第二阶段开发计划

> 文档状态：执行基线草案  
> 编制日期：2026-08-04  
> 当前基线：v0.3.0 第一阶段交付版  
> 第二阶段目标：v0.6.0 应用生态与工程化版本  
> 适用范围：v0.3.0 基线收口、v0.4.0、v0.5.0、v0.6.0  
> 预计工作量：单人全职约 96～143 人日
> 当前进度：S0 基线验证完成；S1 原生模块注册表、资源管理器、最后语义、不可变字节串与编码完成；S2 能力与权限上下文第一切片完成

## 1. 文档目的

第一阶段已经证明华言是一门能够独立执行中文源码的脚本语言：

~~~text
华言源码
  → Lexer
  → Parser / AST
  → 名称解析与字节码编译
  → 华言字节码
  → 华言 VM
~~~

v0.3.0 已经能够运行：

~~~bash
huayan hello.hua
huayan examples/图书馆/主.hua
~~~

第二阶段不再以“增加更多语法”为中心，而是解决一门脚本语言从可运行到可开发中小型真实应用所缺少的能力：

- 可复现、可追踪的版本基线；
- 清晰可扩展的原生模块和资源模型；
- SQLite 数据库；
- 受控的同步 HTTP 客户端和服务器；
- 项目清单、依赖锁定和本地/Git 包依赖；
- LSP 和编辑器体验；
- 更强的测试、性能、安全和兼容保障；
- 使用 SQLite 和 HTTP 升级图书馆系统。

第二阶段结束后，用户应能使用华言创建项目、安装依赖、操作 SQLite、编写 HTTP 服务，并在编辑器中获得诊断、跳转、补全和格式化能力。

## 2. 当前基线审计

### 2.1 已确认能力

当前仓库具备：

- UTF-8、BOM、NFC 和 Unicode 标识符处理；
- 中文关键字、全角标点和中文诊断；
- 递归下降与 Pratt Parser；
- AST、局部槽位、Upvalue 和栈式字节码；
- 函数、递归和闭包；
- 列表、字典、记录和对象身份相等；
- 尝试、捕获、抛出和中文调用栈；
- 模块导入、公开导出、缓存和路径边界；
- 控制台、文字、列表、字典、文件、JSON、时间、数学、程序和测试标准模块；
- REPL、check、dis、fmt、test 命令；
- 模块化命令行图书馆系统；
- Linux、Windows、macOS 发布包。

当前发布二进制黑盒验证结果：

~~~text
华言版本：0.3.0
你好世界：通过
斐波那契：通过，输出 55
一致性测试：15 个案例通过
图书馆端到端：通过
核心演示检查：通过
Linux 发布文件 SHA256：匹配
~~~

### 2.2 必须先解决的基线问题

当前 Git 仓库没有提交，全部项目文件仍处于未跟踪状态。本机当前也没有 Go 工具链，无法独立重跑源码级测试。

因此第二阶段的第一个门禁不是添加功能，而是把 v0.3.0 变成可复现、可回退、可比较的正式基线：

1. 安装并固定 Go 1.22.x 工具链；
2. 重跑单元、Race、Vet、覆盖率、Fuzz、一致性和图书馆测试；
3. 复核发布包和源码是否对应；
4. 提交当前代码；
5. 创建 v0.3.0 标签；
6. 保存测试报告和基准数据；
7. 第二阶段所有工作从该标签分支开始。

未完成该门禁前，不应开始大规模运行时重构。

## 3. 第二阶段目标

### 3.1 产品目标

第二阶段结束时，华言应具备以下用户链路。

创建项目：

~~~bash
huayan init 我的项目
cd 我的项目
huayan run
~~~

增加依赖：

~~~bash
huayan add 路径:../公共模块
huayan install
~~~

数据库程序：

~~~text
导入 标准.数据库 为 数据库

让 数据库 = SQLite.打开("图书馆.db")

尝试
    SQLite.执行(
        数据库,
        "INSERT INTO books(id, title) VALUES (?, ?)",
        ["B001", "活着"]
    )
最后
    SQLite.关闭(数据库)
结束
~~~

HTTP 服务：

~~~text
导入 标准.HTTP 为 HTTP

函数 处理请求(请求)
    返回 记录 {
        状态: 200,
        头: {"Content-Type": "application/json"},
        正文: "{\"消息\":\"你好，华言\"}"
    }
结束

让 服务器 = HTTP.创建服务器("127.0.0.1:8080", 处理请求)

尝试
    HTTP.运行服务器(服务器)
最后
    HTTP.关闭服务器(服务器)
结束
~~~

编辑器：

~~~bash
huayan lsp
~~~

需要支持实时诊断、定义跳转、悬停、补全、文档符号和格式化。

图书馆系统：

~~~bash
huayan run --entry 图书馆.入口.命令行
huayan run --entry 图书馆.入口.服务端
~~~

CLI 和 HTTP 入口共享同一套 SQLite 仓库与业务服务。

### 3.2 工程目标

- 标准模块不再集中堆积在单个 VM 文件；
- 原生资源拥有统一生命周期和关闭协议；
- 新增能力不能暴露 Go panic、map 随机顺序和宿主对象；
- v0.3 有效源码在第二阶段保持兼容；
- 包安装可锁定、可校验、可离线复现；
- LSP 使用与规范一致的 Unicode 位置转换；
- 数据库和 HTTP 接口有资源、超时、大小和路径限制；
- 核心功能有一致性、Fuzz、集成和跨平台测试；
- 发布包仍然是独立可执行文件。

## 4. 第二阶段范围

### 4.1 必须完成

- v0.3.0 Git 基线和可复现验证；
- 原生模块注册表和标准库拆包；
- 统一资源对象与显式关闭；
- 尝试语句增加最后分支；
- 不可变字节串类型；
- 引擎能力与权限上下文；
- SQLite 标准模块；
- 同步 HTTP 客户端；
- 单 VM 串行处理的 HTTP 服务器；
- 项目清单和锁文件；
- 本地路径依赖和 Git/HTTPS 依赖；
- 确定性依赖解析和本地缓存；
- huayan init、run、add、install、list；
- huayan lsp；
- 基础 VS Code 扩展；
- 格式化器和测试工具增强；
- 图书馆系统迁移到 SQLite；
- 图书馆 HTTP JSON API；
- 跨平台发布、迁移文档和第二阶段验收。

### 4.2 可选增强

只有必须项按计划完成后才考虑：

- 源文件字节码缓存；
- LSP 全项目引用和安全重命名；
- HTTP TLS 监听；
- Git 依赖的标签自动选择；
- 调试协议原型；
- 基础性能分析命令；
- 图书馆静态网页界面。

### 4.3 第二阶段继续不做

- 类、继承、接口和完整面向对象系统；
- 泛型和完整静态类型系统；
- 宏；
- 协程、异步语法和多线程华言 VM；
- JIT 和原生机器码；
- 自研垃圾回收器；
- 编译器自举；
- 公共中心化包仓库；
- 任意 Go 动态插件加载；
- ORM；
- PostgreSQL 和 MySQL；
- 生产级 Web 框架；
- GUI 框架；
- 多租户、复杂鉴权和云部署平台。

这些能力留给第三阶段或独立项目。第二阶段 HTTP 服务器定位为中小型工具和开发服务，不宣称高并发生产能力。

## 5. 版本路线

### v0.4.0：应用运行时

包含：

- v0.3.0 基线收口；
- 标准库和原生模块架构重构；
- 资源对象；
- 最后语句；
- 字节串；
- SQLite；
- 权限上下文基础；
- 数据库一致性和跨平台测试。

完成标志：华言能够安全地开发 SQLite 持久化的命令行应用。

### v0.5.0：项目与开发工具

包含：

- 项目清单；
- 锁文件；
- 本地路径和 Git 依赖；
- init、run、add、install、list；
- LSP；
- VS Code 扩展；
- 格式化和测试工具增强；
- 项目级缓存和索引。

完成标志：华言从“运行单个脚本”升级为“管理多模块项目”。

### v0.6.0：网络应用与图书馆系统 v2

包含：

- HTTP 客户端；
- 串行 HTTP 服务器；
- 网络权限；
- 图书馆 SQLite 迁移；
- 图书馆 HTTP JSON API；
- 性能、安全和发布加固；
- 第二阶段完整文档。

完成标志：同一套华言业务模块可以同时服务 CLI 和本地 HTTP API。

## 6. 兼容性原则

- v0.3.0 合法源码必须继续运行；
- 新关键字必须经过冲突扫描和 ADR 审查；
- 标准库现有函数不能在 v0.4～v0.6 删除；
- 需要替换的 API 先弃用至少两个次版本；
- 新增诊断编号，不能复用旧编号表示不同错误；
- 模块根目录规则保持兼容；
- 字节码仍是内部格式，不承诺跨次版本加载；
- 项目清单是可选能力，直接执行 huayan 文件.hua 继续有效；
- 无项目清单时保持 v0.3 的文件和环境访问行为；
- 使用 huayan run 时按项目权限清单执行。

每个用户可见变化必须同时更新：

- docs/spec；
- docs/兼容政策.md；
- 一致性测试；
- Release Notes；
- 必要时提供迁移工具。

## 7. 运行时重构

### 7.1 当前问题

当前值模型、VM、原生函数和标准模块主要集中在 internal/vm/vm.go。继续加入 SQLite、HTTP、包和资源能力会导致：

- 单文件持续膨胀；
- 标准模块互相耦合；
- 原生资源没有统一生命周期；
- 测试边界不清晰；
- 权限检查难以统一；
- 后续嵌入式使用难以维护。

### 7.2 目标包结构

~~~text
internal/
├── runtime/
│   ├── value/
│   ├── object/
│   ├── resource/
│   └── errors/
├── vm/
├── native/
│   ├── registry/
│   ├── context/
│   └── binding/
├── stdlib/
│   ├── console/
│   ├── text/
│   ├── list/
│   ├── dict/
│   ├── file/
│   ├── json/
│   ├── time/
│   ├── math/
│   ├── process/
│   ├── test/
│   ├── sqlite/
│   └── http/
├── capability/
├── project/
├── package/
└── lsp/
~~~

该结构是职责目标，不要求一次性机械搬迁。每次移动必须保证一致性测试始终通过。

### 7.3 原生模块注册表

标准模块通过注册表暴露：

~~~text
模块名称
模块版本
导出函数集合
所需能力
初始化函数
关闭函数
~~~

原生函数接口必须提供：

- VM 调用上下文；
- 参数数量与类型验证工具；
- 华言值构造工具；
- 当前位置和模块信息；
- 权限检查；
- 资源注册；
- 华言错误构造；
- panic 隔离。

第二阶段只保证仓库内部的 Go 原生模块接口，不承诺任意第三方编译后的 Go 插件 ABI。

### 7.4 引擎上下文

Engine 和 VM 需要共享明确的执行上下文：

~~~text
项目根目录
当前模块路径
命令行参数
标准输入、输出和错误输出
环境变量视图
能力集合
取消信号
资源管理器
模块缓存
包解析器
时间和随机数提供器
~~~

测试可以替换时间、环境、文件根和网络客户端，使标准库测试不依赖真实外部状态。

## 8. 语言增量

第二阶段只增加解决资源、数据库和网络问题所必需的语义。

### 8.1 最后分支

语法：

~~~text
尝试
    执行操作()
捕获 原因
    处理错误(原因)
最后
    释放资源()
结束
~~~

也允许：

~~~text
尝试
    执行操作()
最后
    释放资源()
结束
~~~

规则：

- 尝试必须至少包含捕获或最后之一；
- 最后无论成功、捕获、再次抛出都必须执行；
- 最后中再次抛出的错误成为主错误，原错误记录为原因；
- 为避免控制流覆盖，第一版禁止在最后中使用返回、跳出和继续；
- 嵌套最后按后进先出执行；
- 最后中的未捕获错误必须包含原始错误链；
- 编译器需要为返回和抛出生成清理路径；
- 字节码验证器必须验证错误处理区和清理跳转。

不新增 defer，避免同时存在两套资源清理模型。

### 8.2 字节串

新增不可变字节串值，用于：

- HTTP 请求和响应体；
- 文件二进制读取；
- 哈希和校验；
- SQLite BLOB；
- 编码转换。

规则：

- 字节串与文字是不同类型；
- 字节串长度按字节计算；
- 下标返回 0～255 的整数；
- 字节串按内容相等；
- 字节串不可直接与文字拼接；
- 必须显式 UTF-8 编码和解码；
- JSON 默认不接受字节串，必须先 Base64 或转换。

新增标准模块：

~~~text
标准.编码
  UTF8编码
  UTF8解码
  Base64编码
  Base64解码
  十六进制编码
  十六进制解码

标准.文件
  读取字节
  写入字节
~~~

第二阶段不新增独立字节字面量语法，避免词法层发生不必要变化。

### 8.3 资源对象

新增只能由原生模块创建的资源对象：

~~~text
数据库连接
数据库事务
HTTP 响应体
监听器
文件流
~~~

规则：

- 资源按对象身份比较；
- 资源不能 JSON 序列化；
- 资源不能作为字典键；
- 关闭操作必须幂等；
- 使用已关闭资源产生资源错误；
- 资源离开作用域不会立刻保证关闭；
- VM 退出时资源管理器进行兜底关闭；
- 垃圾回收终结器只做泄漏保护，不能替代显式关闭；
- 调试模式记录资源创建位置并报告泄漏。

### 8.4 暂不增加的语法

第二阶段不增加：

- 匿名函数新语法；
- 类；
- 模式匹配；
- 解构赋值；
- 默认参数；
- 可变参数；
- async/await；
- 运算符重载。

HTTP 处理器和事务回调使用已有的命名函数。

## 9. 权限和能力模型

### 9.1 能力集合

第二阶段定义：

~~~text
文件读取
文件写入
环境读取
网络客户端
网络监听
数据库
子进程
~~~

第二阶段不提供子进程标准模块，但保留能力名称，防止以后无兼容规划地开放。

### 9.2 兼容行为

- huayan 文件.hua 默认保持 v0.3 权限行为；
- huayan run 根据项目清单执行；
- 项目依赖不能扩大根项目声明的权限；
- 缺少权限时返回华言权限错误；
- check、fmt 和 lsp 不执行用户代码；
- 包安装不运行依赖中的脚本；
- 测试命令可以通过参数建立临时文件和网络权限。

### 9.3 文件权限

文件能力可以限定：

~~~text
读取路径
写入路径
是否允许符号链接
最大文件大小
~~~

路径必须在解析符号链接后再次检查边界。

### 9.4 网络权限

网络能力可以限定：

~~~text
允许的主机
允许的端口
是否允许监听
请求超时
最大响应体
~~~

默认 HTTP 服务器只允许绑定 127.0.0.1。绑定 0.0.0.0 必须显式声明网络监听权限。

## 10. SQLite 标准模块

### 10.1 实现选择

优先选择不依赖 CGO 的纯 Go SQLite 驱动，以保留当前单二进制和交叉编译能力。

S3 开始前必须通过 ADR 比较：

- 纯 Go 驱动的兼容性、二进制体积和性能；
- CGO 驱动的成熟度与跨平台成本；
- Linux、Windows、macOS 构建稳定性；
- SQLite 版本和安全更新策略。

如果纯 Go 方案不能通过正确性和跨平台测试，再重新评估 CGO；不能在没有 ADR 的情况下临时切换。

### 10.2 API 草案

~~~text
标准.数据库
  打开(路径, [选项]) -> 数据库资源
  关闭(数据库)
  执行(数据库或事务, SQL, [参数])
  查询(数据库或事务, SQL, [参数]) -> 记录列表
  查询一行(数据库或事务, SQL, [参数]) -> 记录或空
  开始事务(数据库) -> 事务资源
  提交(事务)
  回滚(事务)
  事务(数据库, 函数) -> 函数返回值
  最后插入编号(执行结果)
  受影响行数(执行结果)
~~~

调用示例：

~~~text
导入 标准.数据库 为 数据库

让 数据库 = SQLite.打开("数据/图书馆.db")

尝试
    SQLite.执行(
        数据库,
        "CREATE TABLE IF NOT EXISTS books (" +
        "id TEXT PRIMARY KEY, title TEXT NOT NULL)"
    )

    SQLite.执行(
        数据库,
        "INSERT INTO books(id, title) VALUES (?, ?)",
        ["B001", "活着"]
    )

    让 图书 = SQLite.查询一行(
        数据库,
        "SELECT id, title FROM books WHERE id = ?",
        ["B001"]
    )

    打印(图书.title)
最后
    SQLite.关闭(数据库)
结束
~~~

SQL 列名按查询顺序写入记录。重复列名必须要求 SQL 使用别名，否则报告数据库错误。

### 10.3 类型映射

| SQLite | 华言 |
|---|---|
| NULL | 空 |
| INTEGER | 整数 |
| REAL | 小数 |
| TEXT | 文字 |
| BLOB | 字节串 |

整数超出华言 int64 的情况必须报告错误，不能静默转换为小数。

### 10.4 数据库安全

- 所有动态值必须使用参数绑定；
- 文档禁止字符串拼接生成动态 SQL 值；
- 查询参数只能从华言值安全映射；
- 设置最大返回行数和查询超时；
- 数据库路径受项目文件权限约束；
- 错误不能泄漏宿主 Go 堆栈；
- 事务在错误展开时必须回滚；
- 连接关闭前处理未完成事务；
- 并发模型保持单 VM 串行调用；
- 测试数据库使用临时目录。

### 10.5 SQLite 验收

- 建表、增删改查；
- 空值、整数、小数、文字和字节串映射；
- 参数绑定阻止 SQL 注入；
- 提交和回滚；
- 回调事务遇到华言错误自动回滚；
- 关闭后访问产生资源错误；
- 数据库损坏产生可捕获错误；
- Windows 路径和中文文件名；
- 发布包不依赖外部动态库；
- 图书馆核心仓库测试通过。

## 11. HTTP 标准模块

### 11.1 并发边界

第二阶段不实现多线程华言 VM 和协程。

HTTP 服务器可以由 Go 接收连接，但所有华言处理器调用必须进入单一串行执行队列。这样可以保证：

- 华言可变对象不发生数据竞争；
- 模块变量和数据库连接保持确定语义；
- 不引入新的并发内存模型；
- Race 测试可以验证宿主边界。

该模型适合本地服务、管理工具和教学应用，不适合高并发公网服务。

### 11.2 HTTP 客户端 API

~~~text
标准.HTTP
  请求(方法, URL, [选项]) -> 响应记录
  获取(URL, [选项])
  发送JSON(方法, URL, 值, [选项])
~~~

选项记录：

~~~text
记录 {
    头: {"Authorization": "Bearer ..."},
    正文: 字节串或文字,
    超时毫秒: 5000,
    最大响应字节: 1048576,
    跟随重定向: 假
}
~~~

响应记录：

~~~text
记录 {
    状态: 200,
    头: 字典,
    正文: 字节串,
    URL: "http://..."
}
~~~

### 11.3 HTTP 服务器 API

~~~text
标准.HTTP
  创建服务器(地址, 处理函数, [选项]) -> 服务器资源
  运行服务器(服务器资源)
  停止服务器(服务器资源)
  关闭服务器(服务器资源)
  JSON响应(状态, 值)
  文字响应(状态, 文字)
~~~

请求记录：

~~~text
记录 {
    方法: "GET",
    路径: "/api/books",
    查询: 字典,
    头: 字典,
    正文: 字节串,
    远端地址: "127.0.0.1:50000"
}
~~~

处理器返回：

~~~text
记录 {
    状态: 200,
    头: {"Content-Type": "application/json; charset=utf-8"},
    正文: 字节串或文字
}
~~~

### 11.4 服务器限制

- 默认绑定 127.0.0.1；
- 创建服务器只分配监听资源，运行服务器进入阻塞的串行请求循环；
- 停止服务器请求循环退出，关闭服务器幂等释放监听资源；
- 默认请求头和正文大小上限；
- 读取、写入和空闲超时；
- 不允许处理器直接访问 Go 请求对象；
- 处理器未捕获错误返回 500，并记录华言调用栈；
- 响应头名称和值必须验证；
- 阻止响应拆分；
- 路径解析不能自动映射本地文件；
- 静态文件服务必须是独立、受根路径约束的 API；
- 默认不信任代理头；
- v0.6 不承诺 HTTP/2、自定义 TLS 和 WebSocket。

### 11.5 HTTP 验收

- GET、POST、查询参数、请求头和 JSON；
- 超时和取消；
- 请求体、响应体大小限制；
- 无权限网络访问失败；
- 默认不能绑定 0.0.0.0；
- 串行处理器无数据竞争；
- 处理器异常返回稳定错误；
- 正常停止释放监听资源；
- 图书馆 API 端到端测试通过。

## 12. 项目清单

### 12.1 文件格式

采用 UTF-8 JSON，文件名为 huayan.json。原因：

- 华言已有稳定 JSON 模块；
- 不需要立即引入 TOML/YAML 解析器；
- 可以用记录模型自然表示；
- 跨平台工具成熟；
- 便于校验和锁定。

示例：

~~~json
{
  "名称": "图书馆",
  "版本": "0.6.0",
  "入口": "图书馆.入口.命令行",
  "华言版本": ">=0.6.0 <0.7.0",
  "源目录": "src",
  "测试目录": "tests",
  "依赖": {
    "公共工具": {
      "路径": "../公共工具"
    }
  },
  "权限": {
    "文件读取": ["数据"],
    "文件写入": ["数据"],
    "数据库": true,
    "网络客户端": false,
    "网络监听": ["127.0.0.1:8080"]
  }
}
~~~

字段名采用中文，工具内部使用稳定字段编号或常量。未知字段默认警告，格式版本不支持时必须拒绝。

### 12.2 锁文件

锁文件名为 huayan.lock，包含：

- 清单格式版本；
- 华言版本；
- 每个依赖的精确版本或提交；
- 来源；
- 内容 SHA256；
- 依赖图；
- 生成平台无关信息。

锁文件必须确定性排序。相同清单和依赖源应生成完全相同的锁文件。

### 12.3 模块解析

解析顺序：

1. 当前包模块；
2. 清单中声明的依赖包；
3. 标准模块。

禁止：

- 未声明依赖的隐式搜索；
- 依赖名称冲突；
- 包路径逃逸；
- 依赖包覆盖标准模块；
- 循环包依赖；
- 依赖通过符号链接逃出缓存根。

已有无清单项目继续使用 v0.3 的项目根模块解析。

## 13. 包和依赖管理

### 13.1 第二阶段支持的来源

- 本地路径；
- Git HTTPS URL；
- 指定 Git 提交；
- 指定 Git 标签。

第二阶段不建设公共中心仓库，不允许未锁定的任意安装脚本。

### 13.2 命令

~~~bash
huayan init 项目名
huayan run
huayan run --entry 模块.入口
huayan add 路径:../公共模块
huayan add git:https://example.com/name/repo.git#v1.2.0
huayan install
huayan list
huayan remove 包名
huayan update 包名
~~~

中文别名可以在 API 稳定后增加，不作为 v0.5 阻塞项。

### 13.3 缓存

- 使用操作系统规范的用户缓存目录；
- 缓存键包含来源、提交和内容哈希；
- 安装先进入临时目录，验证后原子移动；
- 清理不能删除当前项目正在使用的依赖；
- 离线模式只使用锁文件和本地缓存；
- 缓存损坏时重新下载或给出可操作错误。

### 13.4 供应链安全

- Git 依赖必须锁到提交；
- 下载内容必须计算 SHA256；
- 锁文件校验失败必须停止；
- 拒绝路径穿越和符号链接逃逸；
- 默认不运行依赖脚本；
- 依赖不能修改项目文件；
- 凭据不能写入锁文件；
- 日志不能输出 URL 中的令牌；
- 支持只读离线安装验证。

### 13.5 包管理验收

- 新建项目后可以运行；
- 本地路径依赖可以导入；
- Git 依赖锁定到确定提交；
- 删除网络后可以根据缓存和锁文件安装；
- 锁文件重复生成完全一致；
- 依赖环有完整链路；
- 校验失败不执行代码；
- Windows、Linux、macOS 路径行为一致。

## 14. LSP 与编辑器

### 14.1 LSP 目标

huayan lsp 第一版必须支持：

- initialize 和 shutdown；
- textDocument/didOpen；
- textDocument/didChange；
- textDocument/didClose；
- publishDiagnostics；
- textDocument/hover；
- textDocument/definition；
- textDocument/completion；
- textDocument/documentSymbol；
- textDocument/formatting；
- workspace/didChangeWatchedFiles。

可选增强：

- references；
- rename；
- signatureHelp；
- semanticTokens；
- workspaceSymbol。

### 14.2 Unicode 位置

华言内部使用 UTF-8 字节偏移和 Unicode 码点列，LSP 默认使用 UTF-16 Code Unit。

必须建立统一位置转换层，专项测试：

~~~text
汉字
Emoji
组合字符
CRLF
混合换行
行尾位置
文件末尾
~~~

任何模块不得自行重复实现位置转换。

### 14.3 分析架构

~~~text
文档快照
  → Source Manager
  → Lexer / Parser
  → 符号索引
  → 模块图
  → 诊断、定义、悬停和补全
~~~

第一版可以整文件重新解析，不要求增量 Parser。必须通过缓存和取消避免旧分析覆盖新结果。

LSP 不执行用户代码，不初始化运行时模块，不进行网络和数据库操作。

### 14.4 补全和悬停

补全来源：

- 关键字；
- 当前作用域名称；
- 当前模块公开名称；
- 导入模块导出；
- 标准库函数；
- 记录字段的局部推断；
- 项目依赖的公开符号。

悬停至少显示：

- 名称类别；
- 声明位置；
- 函数参数数量；
- 标准库文档；
- 是否公开；
- 可确定时显示值类型。

第二阶段不承诺完整静态类型推断。

### 14.5 VS Code 扩展

目录建议：

~~~text
editors/vscode/
├── package.json
├── language-configuration.json
├── syntaxes/huayan.tmLanguage.json
└── src/extension.ts
~~~

能力：

- .hua 和 .华 文件关联；
- 语法高亮；
- 中文关键字补全；
- 启动 huayan lsp；
- 保存时格式化；
- 运行当前文件；
- 显示诊断；
- 不内置另一套语言解析器。

### 14.6 LSP 验收

- 错误输入时服务不崩溃；
- 中文和 Emoji 后的诊断位置正确；
- 跨模块定义跳转；
- 文件修改后旧诊断被清除；
- 标准库补全和悬停；
- 格式化与 CLI 一致；
- 100 个模块项目可交互使用；
- 取消旧请求后不发布过期结果。

## 15. 格式化、检查和测试工具

### 15.1 格式化器

第二阶段将保守格式化器升级为基于 Token/AST 的格式化器：

- 统一缩进；
- 统一逗号和空格；
- 保护注释位置；
- 保留字符串内容；
- 支持目录递归；
- 支持标准输入输出；
- 支持 --check；
- 重复格式化结果不变；
- 语法错误文件默认不重写。

### 15.2 检查器

huayan check 增加：

- 未使用局部变量；
- 不可达代码；
- 永真/永假常量条件；
- 重复导入；
- 未使用导入；
- 模块公开名称冲突；
- 标准库函数参数数量；
- 项目清单和权限检查；
- 可选 JSON 诊断输出。

这些检查不能变成不可靠的完整类型检查器。无法确定时不报错误。

### 15.3 测试命令

huayan test 增加：

- 测试名称过滤；
- 单文件和目录；
- 失败继续；
- 成功、失败、跳过统计；
- 每个测试超时；
- 临时目录；
- 随机顺序和固定种子；
- JSON 报告；
- 网络和数据库权限控制；
- 退出码稳定。

## 16. 图书馆系统 v2

### 16.1 目标

图书馆系统 v2 用来证明：

- SQLite 参数绑定和事务；
- 资源清理；
- 项目清单与权限；
- 多入口项目；
- HTTP 客户端和服务器；
- LSP 的跨模块分析；
- 新测试工具；
- v0.3 JSON 数据迁移。

语言核心不能出现图书馆专用语法或 API。

### 16.2 目录

~~~text
examples/图书馆/
├── huayan.json
├── huayan.lock
├── src/
│   └── 图书馆/
│       ├── 入口/
│       │   ├── 命令行.hua
│       │   └── 服务端.hua
│       ├── 模型/
│       │   ├── 图书.hua
│       │   ├── 读者.hua
│       │   └── 借阅记录.hua
│       ├── 仓库/
│       │   ├── 数据库.hua
│       │   ├── 图书仓库.hua
│       │   ├── 读者仓库.hua
│       │   └── 借阅仓库.hua
│       ├── 服务/
│       │   ├── 图书服务.hua
│       │   ├── 读者服务.hua
│       │   └── 借阅服务.hua
│       ├── HTTP/
│       │   ├── 路由.hua
│       │   └── 处理器.hua
│       └── 迁移/
│           └── JSON到SQLite.hua
├── tests/
│   ├── 单元/
│   ├── 数据库/
│   └── HTTP/
└── data/
    └── 图书馆.db
~~~

测试和发布包不能提交包含用户隐私的真实数据库。

### 16.3 数据库结构

图书表：

~~~sql
CREATE TABLE books (
    id TEXT PRIMARY KEY,
    title TEXT NOT NULL,
    author TEXT NOT NULL,
    category TEXT NOT NULL,
    publish_year INTEGER,
    status TEXT NOT NULL,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL
);
~~~

读者表：

~~~sql
CREATE TABLE readers (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    contact TEXT,
    status TEXT NOT NULL,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL
);
~~~

借阅表：

~~~sql
CREATE TABLE loans (
    id TEXT PRIMARY KEY,
    book_id TEXT NOT NULL,
    reader_id TEXT NOT NULL,
    borrowed_at TEXT NOT NULL,
    due_at TEXT NOT NULL,
    returned_at TEXT,
    status TEXT NOT NULL,
    FOREIGN KEY(book_id) REFERENCES books(id),
    FOREIGN KEY(reader_id) REFERENCES readers(id)
);
~~~

需要索引：

- books.title；
- books.author；
- loans.book_id；
- loans.reader_id；
- loans.status。

### 16.4 事务规则

借书事务：

1. 检查读者存在且状态正常；
2. 检查读者未达到借阅上限；
3. 检查图书可借；
4. 更新图书状态；
5. 插入借阅记录；
6. 任一步失败全部回滚。

还书事务：

1. 查找有效借阅记录；
2. 更新借阅记录；
3. 更新图书状态；
4. 任一步失败全部回滚。

数据库唯一约束和业务层检查必须同时存在。

### 16.5 JSON 迁移

迁移工具必须：

- 先校验全部旧 JSON；
- 在事务中导入；
- 保留原始 JSON；
- 重复执行不产生重复数据；
- 输出导入、跳过和失败数量；
- 发现冲突时回滚；
- 提供 --dry-run；
- 生成迁移报告。

### 16.6 HTTP API

第一版接口：

~~~text
GET    /health
GET    /api/books
GET    /api/books/{id}
POST   /api/books
PUT    /api/books/{id}
DELETE /api/books/{id}

GET    /api/readers
POST   /api/readers

GET    /api/loans
POST   /api/loans
POST   /api/loans/{id}/return
~~~

响应统一为：

~~~json
{
  "成功": true,
  "数据": {},
  "错误": null
}
~~~

错误响应：

~~~json
{
  "成功": false,
  "数据": null,
  "错误": {
    "类别": "借阅错误",
    "消息": "图书已经借出"
  }
}
~~~

服务端默认只监听 127.0.0.1。可选管理令牌从环境变量读取，不能写入源码、清单或日志。

### 16.7 图书馆 v2 验收

- CLI 所有 v1 功能继续工作；
- JSON 数据可以迁移；
- SQLite 重启后数据保留；
- 借书和还书事务正确；
- SQL 参数绑定覆盖全部用户输入；
- 同一套服务模块被 CLI 和 HTTP 共用；
- HTTP CRUD 和借还接口通过；
- 服务默认不公开监听；
- 数据库故障返回华言错误；
- 中断操作不留下半完成借阅；
- 测试不修改仓库示例数据；
- 可使用发布版 huayan 独立运行。

## 17. 测试策略

### 17.1 基线测试

S0 必须保存：

~~~bash
go test ./...
go test -race ./...
go vet ./...
scripts/check-core-coverage.sh 85
scripts/check-conformance.sh ./huayan
scripts/test-library.sh ./huayan
~~~

并记录：

- Go 版本；
- 操作系统和架构；
- 测试用时；
- 覆盖率；
- 基准结果；
- 构建文件 SHA256。

### 17.2 一致性测试

当前 15 个案例扩展到至少 80 个，覆盖：

- 最后执行顺序；
- 错误链；
- 字节串；
- 资源关闭；
- SQLite 类型和事务；
- 权限；
- 项目清单；
- 包解析；
- HTTP；
- Unicode LSP 位置；
- v0.3 兼容性。

案例数量不是唯一质量指标，每个用户可见规则至少要有正常、边界和错误测试。

### 17.3 Fuzz

新增目标：

- 项目清单；
- 锁文件；
- Git 包归档路径；
- SQLite 参数转换；
- HTTP 头、URL 和正文；
- LSP JSON-RPC；
- UTF-8/UTF-16 位置转换；
- 资源重复关闭；
- 错误链格式化。

Fuzz 输入不得触发 panic、死循环、无限分配或路径逃逸。

### 17.4 集成测试

- 用发布二进制创建项目；
- 安装本地依赖；
- 离线重装；
- SQLite CRUD 和回滚；
- 启动临时 HTTP 服务器并请求；
- 图书馆 JSON 迁移；
- 图书馆 CLI 和 API 共用数据；
- LSP 打开、修改、诊断和跳转；
- VS Code 扩展最小启动测试；
- Linux、Windows、macOS 发布包安装验证。

### 17.5 故障注入

必须模拟：

- 磁盘写满；
- 数据库锁定；
- 数据库损坏；
- 网络超时；
- 请求体超限；
- Git 下载中断；
- 锁文件损坏；
- 缓存目录只读；
- 用户取消；
- 资源关闭失败；
- 模块和包循环；
- HTTP 处理器抛出错误。

## 18. 性能计划

### 18.1 先建立基线

S0 记录：

- hello.hua 启动时间；
- 斐波那契 VM 时间；
- Lexer/Parser 每 MB 吞吐；
- 100 模块检查时间；
- JSON 解析和序列化；
- 图书馆端到端时间；
- 峰值内存；
- 发布二进制体积。

第二阶段开始后，以该数据判断回归，不能只凭主观感觉优化。

### 18.2 性能门槛

建议初始门槛：

- 现有核心基准不得无说明回退超过 15%；
- check 处理 100 个普通模块保持交互可接受；
- LSP 输入后诊断应可取消，旧结果不得覆盖新结果；
- SQLite 批量操作必须支持事务；
- HTTP 必须有并发连接上限和请求队列上限；
- 包安装重复执行应命中缓存；
- 发布二进制体积增长必须在 Release Notes 解释。

具体绝对数值在 S0 选定参考机器后写入性能 ADR。

### 18.3 优化顺序

1. 测量；
2. 修复算法和重复解析；
3. 增加缓存；
4. 减少分配；
5. 优化 Value 表示；
6. 最后才考虑更激进的 VM 技术。

第二阶段不使用 JIT 或 NaN Boxing 作为进度目标。

## 19. 安全计划

重点威胁：

| 领域 | 风险 | 控制 |
|---|---|---|
| 文件 | 路径穿越、符号链接逃逸 | 规范化后再次检查根目录 |
| SQLite | SQL 注入、资源泄漏 | 参数绑定、最后和资源管理器 |
| HTTP 客户端 | SSRF、超大响应 | 主机权限、超时、大小限制 |
| HTTP 服务 | 公开监听、慢请求 | 默认本地、超时、队列和正文限制 |
| 包 | 供应链替换、Zip Slip | 提交锁定、SHA256、路径检查 |
| 日志 | 凭据泄漏 | URL/头脱敏 |
| LSP | 恶意大文件、协议洪水 | 文档和请求大小限制、取消 |
| VM | 恶意字节码、深递归 | 验证器和资源限制 |

发布前必须对包安装、HTTP、SQLite 和路径边界做独立安全审查。

## 20. 新增架构决策记录

第二阶段至少需要：

- ADR-007：原生模块注册与内部 ABI；
- ADR-008：资源对象和最后语义；
- ADR-009：不可变字节串；
- ADR-010：SQLite 驱动选择；
- ADR-011：HTTP 单 VM 串行执行模型；
- ADR-012：项目清单和锁文件；
- ADR-013：包来源、缓存和完整性；
- ADR-014：权限与能力模型；
- ADR-015：LSP UTF-16 位置映射；
- ADR-016：第二阶段兼容与弃用策略；
- ADR-017：性能参考平台和门槛。

ADR 必须写清：

- 背景；
- 决定；
- 被拒绝的方案；
- 兼容影响；
- 测试和迁移要求。

## 21. 仓库新增结构

~~~text
.
├── huayan.json
├── internal/
│   ├── runtime/
│   ├── native/
│   ├── stdlib/
│   ├── capability/
│   ├── project/
│   ├── package/
│   └── lsp/
├── editors/
│   └── vscode/
├── docs/
│   ├── package/
│   ├── lsp/
│   ├── database/
│   ├── http/
│   └── security/
├── tests/
│   ├── compatibility/v0.3/
│   ├── package/
│   ├── lsp/
│   ├── sqlite/
│   ├── http/
│   └── security/
└── examples/
    └── 图书馆/
~~~

重构过程中保留清晰 Git 历史，不把移动、格式化和语义修改混在同一提交中。

## 22. 详细里程碑

### S0：v0.3.0 基线收口

预计：3～5 人日。

任务：

- 安装并固定 Go 1.22.x；
- 运行完整源码测试；
- 核对 README、规范、Release Notes 和发布包；
- 保存覆盖率、Race、Vet、Fuzz 和基准结果；
- 确认 dist 生成策略；
- 创建首次正式提交；
- 创建 v0.3.0 标签；
- 设置 main 分支保护和 CI；
- 建立第二阶段分支。

门禁：

- 工作区干净；
- v0.3.0 标签可从源码复现发布包；
- 三平台 CI 通过；
- 黑盒测试结果与当前记录一致；
- 没有未跟踪的关键源文件。

### S1：运行时、原生模块和能力上下文

预计：10～15 人日。

任务：

- 拆分 Value、Object、RuntimeError；
- 建立原生模块注册表；
- 拆分现有十个标准模块；
- 建立 NativeContext；
- 建立 CapabilitySet；
- 建立 ResourceManager；
- 保持 v0.3 所有标准库 API；
- 增加 panic 隔离和资源泄漏诊断；
- 增加嵌入式 Engine 配置测试。

门禁：

- v0.3 一致性案例全部不变；
- VM 核心不直接依赖 SQLite/HTTP；
- 每个标准模块可以独立测试；
- 原生调用边界无 panic；
- Race 和覆盖率门禁通过。

### S2：最后、字节串和资源语义

预计：8～12 人日。

任务：

- 更新 Lexer/Parser/AST；
- Resolver 检查最后控制流；
- 编译清理跳转；
- VM 执行最后；
- 新增错误原因链；
- 新增字节串 Value；
- 新增编码模块；
- 新增文件字节 API；
- 新增资源对象和幂等关闭；
- 更新格式化器、LSP 语法和规范。

门禁：

- 最后所有控制路径测试通过；
- 原错误与清理错误链正确；
- 字节串 Unicode 转换严格；
- 资源关闭和泄漏测试通过；
- v0.3 源码兼容。

### S3：SQLite

预计：12～18 人日。

任务：

- 完成驱动 ADR；
- 增加数据库资源；
- 实现打开、关闭、执行、查询、查询一行；
- 实现参数绑定和类型映射；
- 实现事务；
- 实现超时、取消和行数限制；
- 增加中文数据库错误；
- 增加跨平台构建；
- 更新标准库文档。

门禁：

- SQLite API 验收全部通过；
- 发布包不要求用户安装额外运行时；
- 图书馆仓库层可以用 SQLite 实现；
- SQL 注入测试通过；
- 事务故障注入通过。

交付版本：v0.4.0。

### S4：项目清单和包依赖

预计：14～20 人日。

任务：

- 实现 huayan.json 解析和模式验证；
- 实现项目根、源目录和入口；
- 实现 huayan.lock；
- 扩展模块解析器支持包；
- 实现本地路径依赖；
- 实现 Git 依赖；
- 实现缓存和离线模式；
- 实现完整性校验；
- 实现 init、run、add、install、list、remove、update；
- 实现权限清单执行；
- 增加供应链安全测试。

门禁：

- 相同输入生成相同锁文件；
- 离线可复现安装；
- 依赖不能越权；
- 缓存和路径逃逸测试通过；
- 无清单项目保持兼容。

### S5：LSP、格式化和项目工具

预计：15～22 人日。

任务：

- 建立文档快照和模块索引；
- 实现 UTF-8/码点/UTF-16 转换；
- 实现诊断、悬停、定义和补全；
- 实现文档符号和格式化；
- 增加请求取消；
- 增强 check 和 test；
- 建立 VS Code 扩展；
- 建立协议和大文件 Fuzz；
- 编写编辑器安装文档。

门禁：

- LSP 必须项全部通过；
- 中文、Emoji、组合字符位置正确；
- 100 模块项目无明显卡顿；
- VS Code 可以启动、诊断、跳转和格式化；
- LSP 不执行用户代码。

交付版本：v0.5.0。

### S6：HTTP 和网络权限

预计：12～18 人日。

任务：

- 实现网络权限；
- 实现 HTTP 客户端；
- 实现请求取消、超时和大小限制；
- 实现创建、运行、停止和关闭组成的串行 HTTP 服务器生命周期；
- 实现处理器请求/响应记录；
- 实现 JSON/文字响应辅助函数；
- 实现监听资源和正常停止；
- 增加本地端到端测试；
- 增加 SSRF、响应拆分和公开监听检查。

门禁：

- HTTP API 验收通过；
- Race 检查通过；
- 默认只监听本机；
- 超时、限流和取消有效；
- 处理器错误不会导致进程崩溃。

### S7：图书馆系统 v2

预计：12～18 人日。

任务：

- 建立项目清单和权限；
- 实现 SQLite Schema 和迁移；
- 实现 JSON 到 SQLite 迁移；
- 重写仓库层；
- 保持服务层业务规则；
- 建立 CLI 入口；
- 建立 HTTP 入口；
- 增加事务测试；
- 增加 API 测试；
- 编写部署和数据恢复文档。

门禁：

- 图书馆 v2 验收全部通过；
- CLI 与 HTTP 共用业务模块；
- 所有写业务处于事务；
- 测试不污染示例数据；
- v1 JSON 可以安全迁移。

### S8：加固、发布和第二阶段验收

预计：10～15 人日。

任务：

- 扩展一致性套件；
- 完成 Fuzz 和故障注入；
- 运行性能基准并修复回归；
- 安全审查；
- 三平台 CI；
- 构建 v0.6.0 发布包；
- 校验和与来源归档；
- 安装验证；
- 完成教程、迁移和 API 文档；
- 更新兼容政策；
- 建立第三阶段候选清单。

门禁：

- 项目完成定义全部满足；
- v0.3 兼容套件通过；
- 核心覆盖率门槛不下降；
- 发布包可独立运行；
- 没有高严重度已知缺陷；
- Release Notes 明确所有边界。

交付版本：v0.6.0。

## 23. 依赖关系

~~~text
S0 基线
  ↓
S1 运行时与原生模块
  ↓
S2 最后、字节串、资源
  ├────────→ S3 SQLite
  ├────────→ S4 项目与包
  └────────→ S6 HTTP

S4 项目与包 ─────→ S5 LSP 与项目工具
S3 SQLite ────────┐
S4 项目与包 ──────┼→ S7 图书馆系统 v2
S6 HTTP ──────────┘

S3、S5、S6、S7
  ↓
S8 加固与 v0.6.0 发布
~~~

S3 和 S4 在 S1/S2 稳定后可以有限并行。S5 可以与 S3 后半段并行。S7 必须等待 SQLite、项目和 HTTP API 稳定。

## 24. 工作量与节奏

| 里程碑 | 人日 |
|---|---:|
| S0 基线收口 | 3～5 |
| S1 运行时重构 | 10～15 |
| S2 语言增量 | 8～12 |
| S3 SQLite | 12～18 |
| S4 项目与包 | 14～20 |
| S5 LSP 与工具 | 15～22 |
| S6 HTTP | 12～18 |
| S7 图书馆 v2 | 12～18 |
| S8 加固发布 | 10～15 |
| 合计 | 96～143 |

考虑设计返工、跨平台问题和外部依赖风险，单人全职建议按 5～8 个月规划；业余开发建议按 10～18 个月规划。

每两周应交付一个可演示纵向结果，示例：

- 标准模块从 VM 拆出但旧程序不变；
- finally 确保资源关闭；
- 华言成功完成 SQLite 事务；
- 项目锁文件离线复现；
- VS Code 跳转到中文函数定义；
- 华言 HTTP 服务返回 JSON；
- 图书馆通过 HTTP 完成一次借书和归还。

## 25. 风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 未建立 v0.3 Git 基线就重构 | 无法比较和回退 | S0 设为硬门禁 |
| vm.go 拆分引入行为变化 | 大量隐蔽回归 | 小步移动，每步运行兼容套件 |
| SQLite 驱动影响跨平台 | 发布包不可用 | ADR 先行，三平台原型验证 |
| 资源清理语义不严谨 | 数据库锁和泄漏 | finally、资源管理器、故障注入 |
| HTTP 引入并发数据竞争 | VM 状态损坏 | 单 VM 串行调用，Race 门禁 |
| 包管理范围过大 | 延误 LSP 和图书馆 | 不建设中心仓库，不执行安装脚本 |
| LSP Unicode 位置错误 | 中文用户体验差 | 统一位置转换层和专项测试 |
| 权限模型破坏旧脚本 | v0.3 用户无法升级 | 直接脚本兼容，项目模式才强制 |
| 新能力导致二进制显著增大 | 分发成本增加 | 记录体积、说明增长、可选构建评估 |
| 图书馆业务污染语言核心 | 产生不可复用 API | 所有业务只依赖通用标准模块 |
| 测试数量增长但质量不足 | 虚假安全感 | 每条规范对应正常、边界、错误测试 |
| 第二阶段再次范围膨胀 | 无法发布 v0.6 | 严守不做清单和版本门禁 |

## 26. 文档交付

第二阶段新增或更新：

- 第二阶段语言增量规范；
- 最后语义规范；
- 字节串与编码规范；
- 资源生命周期规范；
- SQLite API；
- HTTP API；
- 项目清单模式；
- 锁文件格式；
- 包依赖与缓存；
- 权限模型；
- LSP 安装与故障排查；
- VS Code 使用教程；
- 图书馆 v1 到 v2 迁移；
- v0.3 到 v0.4/v0.5/v0.6 迁移；
- 安全指南；
- 性能基准说明；
- v0.4.0、v0.5.0、v0.6.0 Release Notes。

所有公开 API 必须包含：

- 参数；
- 返回值；
- 错误；
- 所需权限；
- 资源生命周期；
- 正常示例；
- 边界示例。

## 27. 第一批执行任务

第二阶段开始后的前 15 项：

1. 安装 Go 1.22.x；
2. 重跑并保存 v0.3 全部测试；
3. 核对发布包 SHA256 和源码版本；
4. 创建 v0.3.0 正式提交和标签；
5. 保存 v0.3 性能基准；
6. 编写 ADR-007 原生模块注册；
7. 编写 ADR-008 资源和最后；
8. 定义 Runtime Value/Object 包边界；
9. 建立 NativeContext 和参数绑定工具；
10. 迁移标准.数学作为第一个拆包试点；
11. 迁移标准.JSON作为复杂拆包试点；
12. 每次迁移运行 v0.3 兼容套件；
13. 建立 ResourceManager；
14. 完成最后语法的规范与测试草案；
15. 完成 SQLite 驱动跨平台技术验证。

前 15 项完成后，才进入 SQLite 正式 API 开发。

## 28. 第二阶段完成定义

第二阶段只有同时满足以下条件才算完成：

- v0.3.0 已提交、打标签且可复现；
- v0.3 合法源码兼容套件全部通过；
- huayan 文件.hua 的直接运行方式保持有效；
- 标准模块从 VM 核心解耦；
- 原生资源可以显式、幂等、安全关闭；
- 最后和错误原因链语义稳定；
- 字节串、编码和二进制文件 API 稳定；
- SQLite 支持参数绑定、查询和事务；
- HTTP 客户端和串行服务器通过安全验收；
- 项目清单和锁文件可确定性生成；
- 本地和 Git 依赖可以离线复现；
- 项目权限可以限制文件、数据库和网络；
- LSP 支持诊断、补全、悬停、跳转、符号和格式化；
- VS Code 扩展可以启动华言 LSP；
- 图书馆系统完成 SQLite 迁移；
- 图书馆 CLI 与 HTTP 共用同一套业务代码；
- 图书馆借书和归还操作具备事务一致性；
- 一致性、Fuzz、Race、Vet、覆盖率和三平台 CI 通过；
- Linux、Windows、macOS 发布包可以独立运行；
- 安装、API、迁移、安全和故障排查文档完整；
- 没有高严重度已知缺陷；
- v0.6.0 已创建正式提交、标签、发布包和校验和。

达到以上条件后，华言将从“可开发单机脚本和命令行应用”进入“具备项目、数据库、网络和编辑器生态的中文应用脚本语言”阶段。
