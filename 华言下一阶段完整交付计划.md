# 华言下一阶段完整交付计划

> 文档状态：执行基线  
> 编制日期：2026-08-04  
> 当前基线：v0.1.0 可运行原型  
> 目标版本：v0.3.0 第一阶段正式交付版  
> 适用范围：完成《华言语言开发计划》M0～M8 的全部任务

## 1. 阶段目标

下一阶段不再以“能运行示例”为完成标准，而是把当前原型推进为可发布、可验证、可用于编写小型真实应用的中文脚本语言。

阶段结束时必须同时交付：

- 稳定的词法、语法、语义和模块规范；
- 自有 Lexer、Parser、Resolver、字节码编译器和栈式 VM；
- 正确的局部槽位、闭包变量和模块变量实现；
- 完整的集合、记录、异常和中文调用栈；
- 控制台、文字、列表、字典、文件、JSON、时间、数学、程序和测试标准库；
- REPL、检查、格式化、反汇编和测试命令；
- 完全使用华言编写的模块化图书馆系统；
- Linux、Windows、macOS 构建产物、校验和与发布说明；
- 完整测试、CI、规范、教程和迁移文档。

目标版本为 v0.3.0。完成本计划后，用户不安装 Go，也可以使用发布包中的 `huayan` 或 `huayan.exe` 运行 `.hua` 文件。

## 2. 范围边界

### 2.1 本阶段必须完成

本阶段覆盖原计划 M0～M8 以及第一阶段完成定义中的全部内容。任何“已有基础实现”的能力都必须补齐规范、错误路径、边界测试和跨平台验证后，才能标记完成。

### 2.2 本阶段继续不做

- 类、继承、接口；
- 泛型和完整静态类型系统；
- 宏和编译期元编程；
- 协程、线程和异步运行时；
- 包管理器和公共包仓库；
- JIT 和原生机器码生成；
- Web 框架、GUI、HTTP；
- SQLite、PostgreSQL、MySQL；
- 编译器自举；
- 自研垃圾回收器；
- Python 或 Go 对象互操作。

上述能力属于 v0.4.0 以后，不得占用本阶段交付时间。

## 3. 当前基线与差距

| 领域 | 当前已有 | 下一阶段必须补齐 |
|---|---|---|
| 工程骨架 | Go 模块、CLI、README、基础目录 | LICENSE、ADR、正式规范、CI、发布脚本 |
| 源码与 Lexer | UTF-8、BOM、中文标识符、关键字、注释、全角标点 | NFC、完整 XID、源码管理器、精确恢复、系统 Fuzz |
| Parser | AST、Pratt 表达式、主要声明和语句 | AST 调试输出、多错误恢复、深度限制、正式文法一致性 |
| Resolver | 未声明、重复声明、控制流位置检查 | 稳定符号表、局部槽位、模块符号、捕获描述、常量赋值静态检查 |
| 字节码 | 内存 Chunk、常量池、反汇编 | 明确编码、源码映射、闭包操作码、字节码验证、限制检查 |
| VM | 栈式执行、函数、递归、环境闭包、运行时错误 | 槽位帧、开放/关闭 Upvalue、最大栈限制、损坏字节码安全失败 |
| 值与集合 | 基本类型、列表、字典、记录、字段和索引 | 包结构拆分、对象身份测试、深度边界、遍历并发修改完整检查 |
| 异常 | 尝试、捕获、抛出、中文调用栈 | 跨模块栈、再次抛出、原生 panic 隔离、错误编号和统一错误对象 |
| 模块 | 基本导入、公开导出、缓存、循环检测 | 项目根规则、完整导入链、路径逃逸防护、模块名一致性检查 |
| 标准库 | 主要模块的基础函数 | API 冻结、参数/错误规范、缺失函数、边界测试、资源生命周期 |
| 工具链 | REPL、check、dis、保守 fmt、test | 完整多行 REPL、真实格式化器、Golden 输出、命令退出码全覆盖 |
| 图书馆 | 单文件 CLI 示例、基本 JSON 持久化 | 按模块拆分、完整业务规则、损坏数据保护、业务与端到端测试 |
| 质量 | Go 单测、Race、Vet、少量 Fuzz/一致性测试 | 85% 核心覆盖率、三平台 CI、基准、发布包、安装验证 |

当前代码可以作为行为原型，但不直接视为最终架构。尤其是名称式运行时环境需要迁移为“Resolver 分配槽位、Compiler 生成局部/闭包操作码、VM 按槽位执行”的正式实现。

## 4. 执行原则

1. 先冻结规范，再修改底层实现。
2. 每项用户可见行为必须先有正常、边界和错误测试。
3. 每个里程碑必须通过对应质量门禁，不能把失败测试留给后续阶段。
4. 兼容当前有效示例；必要的不兼容修改必须增加 ADR 和迁移说明。
5. 图书馆系统只能使用通用语言能力，不能向语言核心加入业务专用功能。
6. Go panic、map 随机顺序和字节长度不能成为华言语义。
7. 优先完成纵向闭环，再扩展同类 API 数量。

## 5. 里程碑总览

| 里程碑 | 目标 | 依赖 | 预计工作量 |
|---|---|---|---|
| N0 | 规范冻结与仓库治理 | 无 | 4～6 人日 |
| N1 | Source、Unicode、Lexer、Parser 加固 | N0 | 7～10 人日 |
| N2 | Resolver、槽位字节码和 VM 重构 | N1 | 12～18 人日 |
| N3 | 函数、闭包、集合和异常完成 | N2 | 10～15 人日 |
| N4 | 模块系统与标准库完成 | N3 | 10～15 人日 |
| N5 | REPL、检查、格式化、测试工具完成 | N4 | 7～10 人日 |
| N6 | 模块化图书馆系统完成 | N4、N5 | 8～12 人日 |
| N7 | 测试、性能、安全和三平台 CI | N1～N6 | 8～12 人日 |
| N8 | 文档、发布包和最终验收 | N7 | 5～8 人日 |

总剩余工作量按单人全职估计为 71～106 人日。应以门禁通过为准，不以日期强行宣布完成。

## 6. N0：规范冻结与仓库治理

### N0-01 正式规范落盘

创建并完成：

- `docs/spec/词法规范.md`；
- `docs/spec/语法规范.md`；
- `docs/spec/语义规范.md`；
- `docs/spec/模块规范.md`；
- `docs/spec/标准库规范.md`。

规范必须明确：运算符优先级、整数除法结果、字典缺失键行为、记录字段新增规则、作用域边界、循环变量生命周期、闭包捕获、模块根目录和错误传播。

验收：规范中不存在影响 N1～N4 的“待定”项。

### N0-02 架构决策记录

至少增加以下 ADR：

- ADR-001：采用栈式字节码 VM；
- ADR-002：局部槽位与 Upvalue 捕获模型；
- ADR-003：模块根目录和模块缓存；
- ADR-004：动态强类型和条件必须为布尔；
- ADR-005：Go GC 作为第一代内存管理；
- ADR-006：JSON 对象映射为记录并保持字段顺序。

### N0-03 工程治理

- 增加 LICENSE；
- 增加贡献说明和代码风格；
- 固定 Go 最低版本；
- 增加 `Makefile` 或跨平台任务入口；
- 建立 `docs/decisions`、`docs/tutorials`、`tests/errors`、`tests/integration`、`tests/fuzz`；
- 增加版本常量的唯一来源，避免 CLI 和文档重复维护。

### N0 门禁

```bash
go test ./...
go vet ./...
```

所有规范和 ADR 完成审阅，后续语义修改必须先更新规范。

## 7. N1：源码、Unicode、Lexer 和 Parser 加固

### N1-01 Source Manager

- 实现文件缓存和稳定 SourceID；
- 支持真实文件和 REPL 虚拟文件；
- 保存原始字节偏移、行号、Unicode 字符列；
- 提供行文本、上下文片段和下划线范围；
- 统一 CRLF、CR、LF 的位置计算；
- 对无效 UTF-8 给出可恢复诊断。

### N1-02 Unicode 标识符

- 标识符比较前执行 NFC；
- 使用 Unicode XID_Start/XID_Continue 规则；
- 保留原始拼写用于诊断；
- 明确并测试下划线、拉丁字母、汉字、组合字符和非法 Emoji；
- 关键字在规范化后识别。

### N1-03 Lexer 完成

- 覆盖所有关键字、操作符、全角标点和字面量；
- 多行注释中的换行必须正确影响行号；
- 换行续行规则与括号深度完全一致；
- 字符串转义限定为规范允许集合；
- 整数、小数边界和错误 Token 可恢复；
- 增加 Token 调试输出命令或测试工具。

### N1-04 Parser 完成

- 正式文法与 Parser 一一对应；
- 补齐所有 AST 节点源码范围；
- 增加 AST 调试打印与 Golden 测试；
- 在换行、`结束`、`否则`、`捕获`处同步恢复；
- 一次检查尽可能报告多个语法错误；
- 对极深括号、极深代码块和超长参数列表设置限制；
- 非法输入不得 panic、死循环或无限分配。

### N1 测试

- Source 行列与中文字符专项测试；
- NFC 等价标识符测试；
- 每类 Token 正常、边界、错误测试；
- 表达式优先级和结合性测试；
- 缺括号、缺 `结束`、缺参数等恢复测试；
- Lexer 与 Parser Fuzz，每次 CI 运行固定种子集。

### N1 门禁

- 词法和语法规范示例全部通过；
- Golden 输出稳定；
- Fuzz 至少各运行 60 秒无崩溃；
- Parser 对错误文件能报告两个以上独立错误时，不因第一个错误停止。

## 8. N2：Resolver、字节码和 VM 正式架构

### N2-01 符号解析

- 建立模块、函数、块三级作用域；
- 给局部变量和参数分配稳定槽位；
- 区分模块变量、局部变量和 Upvalue；
- 标记被捕获变量；
- 生成闭包捕获描述；
- 检查重复声明、初始化前读取和未声明赋值；
- 常量重新赋值在执行前报错；
- 检查公开声明只能位于模块顶层；
- 检查返回、跳出、继续的位置。

### N2-02 字节码定义

完成并冻结 v0.3 内部指令表：

- 常量和栈；
- 局部槽位读写；
- Upvalue 读写和关闭；
- 模块变量读写；
- 运算和短路逻辑；
- 跳转、循环和回填；
- 函数、闭包、调用和返回；
- 集合、索引和字段；
- 模块导入和导出；
- 捕获区和抛出。

每条指令必须定义栈前置条件、栈效果、操作数格式和失败行为。

### N2-03 字节码编译器

- AST 按 Resolver 绑定生成槽位指令；
- 删除最终运行路径中的名称式局部变量查找；
- 正确回填条件、循环、跳出和继续；
- 保存每条指令到 SourceSpan 的映射；
- 安全执行常量折叠；
- 函数原型独立常量池；
- 检查单函数局部数、常量数和字节码长度限制。

### N2-04 VM 调用帧与验证器

- 调用帧包含闭包、IP、栈基址、模块和捕获区；
- VM 使用槽位访问局部变量；
- 执行前验证指令、常量索引、跳转地址和栈效果；
- 配置最大操作数栈、调用深度、局部数和字节码长度；
- 损坏字节码返回内部错误，不触发 Go panic；
- 整数加减乘和取负均检查溢出；
- 混合数字运算符合正式规范。

### N2 门禁

- 算术、变量、常量、条件和循环一致性测试通过；
- 反汇编显示槽位、常量、跳转和源码位置；
- 损坏字节码 Fuzz 无崩溃；
- 基础 VM 不再依赖名称式环境执行局部变量。

## 9. N3：函数、闭包、集合和异常完成

### N3-01 函数和闭包

- 参数严格计数；
- 隐式返回空；
- 递归和相互引用规则写入规范；
- 开放 Upvalue 指向栈槽；
- 函数返回时关闭 Upvalue 并提升到堆；
- 多个闭包共享同一绑定单元；
- 多层捕获、写捕获和递归闭包正确；
- 调用栈溢出产生华言错误。

### N3-02 值和对象层

把值、对象和 VM 职责拆分为清晰包，至少区分：

- Value 与类型标签；
- 列表、字典、记录对象；
- 函数原型、闭包、原生函数；
- 模块和错误对象；
- 有序字段和有序字典遍历。

对象相等必须按身份，基本值和混合数字按规范比较。

### N3-03 集合和记录

- 列表索引、赋值、追加、移除和遍历；
- 列表遍历期间修改长度报错；
- 字典只接受布尔、整数和文字键；
- 字典保持确定遍历顺序；
- 记录保持字段插入顺序；
- 点访问和文字索引语义一致；
- 字符串索引和长度按 Unicode 码点；
- 越界、缺失字段、非法键均有中文错误编号。

### N3-04 异常

- 错误对象包含类别、编号、消息、位置和调用栈；
- 只能抛出错误对象；
- 捕获后继续、再次抛出和嵌套捕获正确；
- 跨函数、跨模块展开调用栈；
- 原生函数 panic 转换为内部错误；
- 未捕获错误使用退出码 1；
- 捕获不能吞掉解释器内部损坏。

### N3 门禁

- 斐波那契、计数器、多闭包共享变量通过；
- 集合和记录正常、边界、错误测试通过；
- 未捕获错误显示完整华言帧，不显示 Go 栈；
- JSON 所需值模型冻结。

## 10. N4：模块系统和标准库完成

### N4-01 模块解析

- 一个文件对应一个模块；
- 模块名与项目根相对路径一致；
- 标准模块使用 `标准.` 前缀；
- 用户模块只能在项目根内解析；
- 规范化路径并阻止 `..`、符号链接等逃逸；
- 模块顶层只执行一次；
- 已加载模块缓存；
- 循环导入在执行顶层代码前报告；
- 错误包含完整导入链；
- 仅公开名称可读取。

### N4-02 原生函数接口

- 统一参数数量和类型检查；
- 原生函数返回 Value 或华言错误；
- 能获得当前调用 SourceSpan；
- 禁止直接访问 VM 私有栈；
- 文件句柄有明确关闭机制；
- 所有 panic 在边界转换；
- 原生函数错误消息和编号统一。

### N4-03 标准库 API 冻结

完成以下模块并为每个函数增加文档、参数、返回值、错误条件、示例和测试：

- `标准.控制台`：输出、错误输出、读取一行；
- `标准.文字`：长度、查找、替换、分割、裁剪、大小写；
- `标准.列表`：排序、查找、映射、过滤；
- `标准.字典`：键、值、条目、包含键；
- `标准.文件`：读取、写入、追加、存在、创建目录、原子写入；
- `标准.JSON`：解析、序列化、格式化、循环检测、字段顺序；
- `标准.时间`：现在、解析、格式化、日期差；
- `标准.数学`：绝对值、最小值、最大值、取整；
- `标准.程序`：参数、环境信息、受控退出；
- `标准.测试`：断言相等、断言错误、注册和运行。

### N4-04 跨平台行为

- Linux、Windows、macOS 路径和换行测试；
- UTF-8 文件名测试；
- 原子写入在三平台采用正确替换策略；
- Windows 文件占用和重命名失败返回可理解错误；
- 时间输出包含时区。

### N4 门禁

- 多文件程序和跨模块异常通过；
- 循环导入、私有名称导入和路径逃逸测试通过；
- 标准库公开函数全部有测试和文档；
- JSON 损坏、循环和大嵌套不会覆盖原文件或崩溃。

## 11. N5：工具链完成

### N5-01 REPL

- 状态跨输入保留；
- 表达式自动回显；
- 函数、条件、循环和尝试支持多行输入；
- 括号内多行表达式支持续行；
- `Ctrl+C` 取消当前输入而不退出；
- `Ctrl+D` 正常退出；
- 错误后可以继续输入；
- REPL 虚拟源码位置稳定。

### N5-02 check/检查

- 检查 Lexer、Parser、Resolver、Compiler；
- 递归检查导入模块但不执行顶层副作用；
- 一次输出多个诊断；
- 使用退出码 2 或 3 区分编译和模块错误。

### N5-03 dis/字节码

- 输出函数、常量池、槽位、Upvalue、跳转目标；
- 显示源码行号；
- 输出稳定，可做 Golden 测试。

### N5-04 fmt/格式化

- 基于 Token/AST 格式化，不做危险文本替换；
- 统一缩进、空格、换行和标点；
- 默认移除分号；
- 保持字符串和注释内容；
- 连续运行结果完全一致；
- 支持标准输出、原地写入和检查模式。

### N5-05 test/测试

- 递归发现 `.hua` 和 `.华`；
- 每个测试文件隔离模块和测试注册表；
- 输出通过、失败和汇总；
- 支持单文件、目录和名称过滤；
- 失败使用非零退出码；
- 支持标准输出和错误输出断言。

### N5-06 CLI 和退出码

完整覆盖：

```text
0  成功
1  未捕获运行时错误或测试失败
2  词法、语法、名称或编译错误
3  模块或文件加载错误
64 命令行参数错误
70 解释器内部错误
```

增加 `--help`、`--version`、`-c`、`--` 参数透传测试。

### N5 门禁

- REPL 多行一致性测试通过；
- Formatter Golden 和幂等测试通过；
- CLI 所有命令、别名和退出码通过集成测试。

## 12. N6：图书馆系统完整交付

### N6-01 模块拆分

按原计划重构：

```text
examples/图书馆/
├── 主.hua
├── 配置.hua
├── 模型/
│   ├── 图书.hua
│   ├── 读者.hua
│   └── 借阅记录.hua
├── 仓库/
│   ├── 图书仓库.hua
│   ├── 读者仓库.hua
│   └── 借阅仓库.hua
├── 服务/
│   ├── 图书服务.hua
│   ├── 读者服务.hua
│   └── 借阅服务.hua
├── 界面/
│   └── 菜单.hua
├── 数据/
│   ├── 图书.json
│   ├── 读者.json
│   └── 借阅记录.json
└── 测试/
    ├── 图书服务测试.hua
    └── 借阅服务测试.hua
```

### N6-02 数据规则

- 图书编号唯一；
- 读者编号唯一；
- 借阅记录编号唯一；
- 图书、读者和借阅输入完整校验；
- 只有可借图书可以借出；
- 只有正常读者可以借书；
- 每位读者最多同时借 5 本；
- 借书同时更新图书和记录；
- 归还必须匹配有效借阅记录；
- 重复归还报业务错误；
- 未归还图书不能删除；
- 借出、应还、归还时间完整；
- 所有业务错误在华言代码中捕获并展示。

### N6-03 持久化安全

- 启动时校验 JSON 结构；
- 损坏 JSON 只读报错，不覆盖；
- 写入临时文件并刷新；
- 原子替换前保留可恢复备份；
- 三个实体文件的一次业务修改要么全部成功，要么报告失败并保留旧数据；
- 路径相对图书馆项目目录，不依赖当前终端目录。

### N6-04 菜单和用户体验

菜单必须提供：

1. 添加图书；
2. 查询图书；
3. 登记读者；
4. 借阅图书；
5. 归还图书；
6. 查看借阅记录；
7. 删除图书；
0. 退出。

输入错误不得终止程序，业务错误显示中文消息后返回菜单。

### N6-05 业务测试

覆盖原计划 12 个验收场景，并增加：

- 空数据首次启动；
- 非法年份和空名称；
- 读者状态异常；
- 应还时间计算；
- 数据目录不存在；
- 临时文件写入失败；
- 备份恢复；
- 从不同工作目录启动。

### N6 门禁

执行：

```bash
huayan examples/图书馆/主.hua
huayan test examples/图书馆/测试
```

完整新增、登记、借阅、归还、退出、重启流程通过，重启后数据保持一致。

## 13. N7：测试、性能、安全和 CI

### N7-01 Go 单元测试

覆盖：Source、Lexer、Parser、Resolver、Compiler、字节码验证器、VM、模块加载器、Formatter 和所有标准模块。

核心包语句覆盖率目标不低于 85%。不能用删除错误分支的方式换取覆盖率。

### N7-02 语言一致性测试

建立以下目录：

```text
tests/conformance/
├── 词法/
├── 语法/
├── 作用域/
├── 数字/
├── 文字/
├── 列表/
├── 字典/
├── 记录/
├── 函数/
├── 闭包/
├── 模块/
├── 错误/
├── 标准库/
└── 命令行/
```

每个案例包含源码、标准输出、标准错误和退出码。

### N7-03 Golden 与 Fuzz

Golden 覆盖 Token、AST、诊断、反汇编和格式化。

Fuzz 覆盖：

- 随机 UTF-8 和非法字节；
- Lexer、Parser；
- 极深嵌套；
- 损坏字节码；
- JSON 大嵌套和循环；
- 模块路径和导入图。

### N7-04 基准和资源限制

- Hello World 启动基准；
- 递归调用基准；
- 列表和字典基准；
- JSON 解析/序列化基准；
- 模块首次和缓存加载基准；
- 大输入内存上限测试；
- 建立性能基线，不以提前微优化替代正确性。

### N7-05 三平台 CI

GitHub Actions 或等价 CI 必须在 Linux、Windows、macOS 执行：

```bash
go test ./...
go test -race ./...   # 支持的平台
go vet ./...
go build ./cmd/huayan
```

并运行语言一致性、CLI 和图书馆端到端测试。

### N7 门禁

- 三平台 CI 全绿；
- 核心覆盖率达到目标；
- 固定 Fuzz 种子全部通过；
- 无用户输入可暴露 Go panic；
- 无高优先级缺陷。

## 14. N8：文档、发布和最终验收

### N8-01 用户文档

完成：

- 五分钟入门；
- 安装说明；
- 语言导览；
- 错误信息说明；
- 模块组织指南；
- 标准库 API；
- 图书馆系统教程；
- Python/Lua 迁移差异；
- 版本兼容政策；
- 常见问题和故障排查。

### N8-02 发布自动化

生成：

- `huayan-v0.3.0-linux-amd64.tar.gz`；
- `huayan-v0.3.0-windows-amd64.zip`；
- `huayan-v0.3.0-darwin-amd64.tar.gz`；
- `huayan-v0.3.0-darwin-arm64.tar.gz`；
- SHA-256 校验文件；
- Release Notes；
- 源码归档。

按实际支持情况增加 Linux ARM64 和 Windows ARM64，但不能用未验证的交叉编译产物宣称支持。

### N8-03 干净环境安装测试

在没有 Go 和 Python 的干净机器或容器中验证：

```bash
huayan --version
huayan examples/你好世界.hua
huayan check examples/斐波那契.hua
huayan test tests/conformance
huayan examples/图书馆/主.hua
```

### N8-04 最终缺陷清零

发布阻塞级别：

- P0：崩溃、数据损坏、安全边界失效；
- P1：规范能力不可用、错误结果、跨平台失败；
- P2：非关键工具或文档缺陷；
- P3：体验优化。

v0.3.0 发布前必须清零 P0 和 P1；P2 必须有明确处置记录。

## 15. 任务依赖与推荐顺序

```text
规范/ADR
  → Source/Lexer/Parser
  → Resolver/槽位字节码/VM
  → 闭包/集合/异常
  → 模块/标准库
  → REPL/check/dis/fmt/test
  → 图书馆系统
  → 一致性测试/CI/性能
  → 文档/发布/最终验收
```

可以并行的工作：

- N1 实现期间可同时编写规范示例和 Golden 预期；
- N3 完成值模型后可并行编写标准库文档；
- N4 API 冻结后可并行开发图书馆模块和标准库测试；
- N6 业务开发期间可并行搭建三平台 CI；
- N7 修复期间可并行完成教程和发布说明。

不能并行提前做的工作：

- Resolver/字节码未冻结前，不扩展大量语法；
- 值模型未冻结前，不完成 JSON 和图书馆持久化；
- 模块规则未冻结前，不重构图书馆目录；
- 一致性测试未通过前，不制作正式发布包。

## 16. 每个任务的完成模板

每个任务关闭前必须同时满足：

- [ ] 规范或 API 文档已更新；
- [ ] 正常路径测试已增加；
- [ ] 边界路径测试已增加；
- [ ] 错误路径测试已增加；
- [ ] 中文诊断包含准确位置；
- [ ] `go test ./...` 通过；
- [ ] `go vet ./...` 通过；
- [ ] 相关一致性测试通过；
- [ ] 没有新增未解释的 Go panic；
- [ ] 示例或迁移说明已更新；
- [ ] 验收命令和结果已记录。

## 17. 每次合并质量门槛

```bash
gofmt -w cmd internal
go test ./...
go test -race ./...
go vet ./...
huayan check examples/核心演示.hua
huayan test tests/conformance
```

另外要求：

- Formatter 检查通过；
- Golden 文件无意外变化；
- 用户可见行为变化附带规范或 ADR；
- 不提交运行生成的数据文件；
- 不降低核心测试覆盖率；
- 不合并已知 P0/P1 缺陷。

## 18. 最终交付物清单

### 源码

- [ ] CLI；
- [ ] Source Manager；
- [ ] Lexer；
- [ ] AST/Parser；
- [ ] Resolver；
- [ ] Bytecode/Verifier/Disassembler；
- [ ] Value/Object；
- [ ] VM/Closure/Error；
- [ ] Module Loader；
- [ ] Native Interface；
- [ ] Standard Library；
- [ ] REPL/Formatter/Test Runner。

### 应用

- [ ] 模块化图书馆系统；
- [ ] 示例数据；
- [ ] 华言业务测试；
- [ ] 完整操作教程。

### 质量

- [ ] Go 单元测试；
- [ ] 语言一致性测试；
- [ ] Golden 测试；
- [ ] Fuzz 测试；
- [ ] 集成和端到端测试；
- [ ] 性能基准；
- [ ] 三平台 CI。

### 文档与发布

- [ ] 正式规范；
- [ ] ADR；
- [ ] 入门和安装文档；
- [ ] 标准库 API；
- [ ] 兼容政策；
- [ ] 三平台发布包；
- [ ] 校验和；
- [ ] Release Notes。

## 19. 最终完成定义

只有下列条件全部成立，下一阶段才算完成：

1. `huayan` 是无需 Go/Python 的独立发布程序；
2. `.hua` 和 `.华` 能直接执行；
3. 源码经过自有 Lexer、Parser、Resolver、Compiler 和 VM；
4. VM 使用正式局部槽位和 Upvalue 模型；
5. 函数、闭包、模块、集合、记录和异常通过一致性测试；
6. 标准库 API 完整、稳定、有文档和边界测试；
7. REPL、check、dis、fmt、test 达到工具链验收标准；
8. 图书馆系统模块化实现全部业务规则并安全持久化；
9. 损坏数据不会被覆盖，未预期错误不会显示 Go panic；
10. Linux、Windows、macOS CI 和发布包验证通过；
11. 核心覆盖率达到 85% 目标；
12. P0、P1 缺陷为零；
13. 新用户能在 15 分钟内运行第一个华言程序；
14. 所有规范、教程、API 和发布文档齐全。

任一条件未满足时，版本只能标记为预览版或候选版，不能宣布第一阶段完成。

## 20. 第一批立即执行任务

下一次开发从以下顺序开始：

1. 建立 `docs/spec` 和 `docs/decisions`；
2. 冻结整数除法、字典缺失键、块作用域和模块根语义；
3. 编写 Resolver 槽位/Upvalue ADR；
4. 为当前行为建立一致性测试基线；
5. 实现 Source Manager 和 NFC/XID；
6. 加固 Parser 错误恢复和 AST Golden；
7. 设计并实现槽位字节码；
8. 实现调用帧和 Upvalue；
9. 迁移现有示例并保证行为不回退；
10. 进入模块和标准库完善阶段。

前四项是下一阶段的启动门禁。在规范和行为基线完成前，不开始大规模 VM 重构。
