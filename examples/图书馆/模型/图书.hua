模块 图书馆.模型.图书

公开 函数 创建(编号, 书名, 作者, 年份)
    如果 编号 == "" 或 书名 == "" 或 作者 == ""
        抛出 错误("图书编号、书名和作者不能为空")
    结束
    如果 年份 < 0 或 年份 > 9999
        抛出 错误("图书年份无效")
    结束
    返回 记录 {
        编号: 编号,
        书名: 书名,
        作者: 作者,
        年份: 年份,
        状态: "可借"
    }
结束

公开 函数 可借(图书)
    返回 图书 != 空 且 图书.状态 == "可借"
结束
