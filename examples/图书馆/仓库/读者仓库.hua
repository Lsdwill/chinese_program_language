模块 图书馆.仓库.读者仓库

导入 标准.文件 为 文件
导入 标准.JSON 为 JSON

常量 路径 = "../数据/读者.json"

公开 函数 读取()
    如果 文件.存在(路径)
        返回 JSON.解析(文件.读取文字(路径))
    结束
    返回 []
结束

公开 函数 保存(读者)
    文件.原子写入(路径, JSON.格式化(读者))
结束
