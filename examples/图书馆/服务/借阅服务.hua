模块 图书馆.服务.借阅服务

从 图书馆.配置 导入 借阅天数, 每位读者最多借阅
从 图书馆.模型.图书 导入 可借
从 图书馆.模型.读者 导入 正常
从 图书馆.模型.借阅记录 导入 创建
导入 标准.时间 为 时间

函数 查找图书(图书, 编号)
    遍历 项 于 图书
        如果 项.编号 == 编号
            返回 项
        结束
    结束
    返回 空
结束

函数 当前借阅数(借阅列表, 读者编号)
    让 数量 = 0
    遍历 项 于 借阅列表
        如果 项.读者编号 == 读者编号 且 项.状态 == "借出"
            数量 = 数量 + 1
        结束
    结束
    返回 数量
结束

公开 函数 借书(图书, 读者, 借阅列表, 图书编号, 读者编号, 时间文字)
    让 目标图书 = 查找图书(图书, 图书编号)
    如果 非 可借(目标图书)
        抛出 错误("图书不存在或当前不可借阅")
    结束
    让 目标读者 = 空
    遍历 项 于 读者
        如果 项.编号 == 读者编号
            目标读者 = 项
        结束
    结束
    如果 非 正常(目标读者)
        抛出 错误("读者不存在或状态异常")
    结束
    如果 当前借阅数(借阅列表, 读者编号) >= 每位读者最多借阅
        抛出 错误("每位读者最多同时借阅 5 本")
    结束
    让 编号 = "借阅-" + 转文字(长度(借阅列表) + 1)
    让 借阅 = 创建(编号, 图书编号, 读者编号, 时间文字, 时间.加秒(时间文字, 借阅天数 * 86400))
    目标图书.状态 = "借出"
    借阅列表.追加(借阅)
    返回 借阅
结束

公开 函数 归还(图书, 借阅列表, 图书编号, 时间文字)
    让 目标 = 空
    遍历 项 于 借阅列表
        如果 项.图书编号 == 图书编号 且 项.状态 == "借出"
            目标 = 项
        结束
    结束
    如果 目标 == 空
        抛出 错误("没有有效的借阅记录")
    结束
    遍历 项 于 图书
        如果 项.编号 == 图书编号
            项.状态 = "可借"
        结束
    结束
    目标.状态 = "已还"
    目标.归还时间 = 时间文字
    返回 目标
结束
