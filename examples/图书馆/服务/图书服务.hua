模块 图书馆.服务.图书服务

从 图书馆.模型.图书 导入 可借

公开 函数 查找(图书, 编号)
    遍历 项 于 图书
        如果 项.编号 == 编号
            返回 项
        结束
    结束
    返回 空
结束

公开 函数 添加(图书, 新图书)
    如果 查找(图书, 新图书.编号) != 空
        抛出 错误("图书编号已经存在")
    结束
    图书.追加(新图书)
    返回 新图书
结束

公开 函数 删除(图书, 编号, 借阅)
    让 目标 = 查找(图书, 编号)
    如果 目标 == 空
        抛出 错误("图书不存在")
    结束
    遍历 借阅记录 于 借阅
        如果 借阅记录.图书编号 == 编号 且 借阅记录.状态 == "借出"
            抛出 错误("未归还图书不能删除")
        结束
    结束
    遍历 索引 于 范围(0, 长度(图书))
        如果 图书[索引].编号 == 编号
            图书.移除(索引)
            返回 真
        结束
    结束
    返回 假
结束
