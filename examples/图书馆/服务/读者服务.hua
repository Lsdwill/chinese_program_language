模块 图书馆.服务.读者服务

公开 函数 查找(读者, 编号)
    遍历 项 于 读者
        如果 项.编号 == 编号
            返回 项
        结束
    结束
    返回 空
结束

公开 函数 添加(读者, 新读者)
    如果 查找(读者, 新读者.编号) != 空
        抛出 错误("读者编号已经存在")
    结束
    读者.追加(新读者)
    返回 新读者
结束
