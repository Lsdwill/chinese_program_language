让 图书 = 记录 {
    编号: "B001",
    书名: "活着",
    状态: "可借"
}

函数 创建计数器()
    让 次数 = 0
    函数 增加()
        次数 = 次数 + 1
        返回 次数
    结束
    返回 增加
结束

让 计数器 = 创建计数器()
打印(图书.书名)
打印(计数器())
打印(计数器())

尝试
    如果 图书.状态 != "可借"
        抛出 错误("图书当前不可借阅")
    结束
    图书.状态 = "借出"
捕获 原因
    打印("操作失败：" + 原因.消息)
结束

遍历 数字 于 范围(1, 4)
    打印(数字)
结束
