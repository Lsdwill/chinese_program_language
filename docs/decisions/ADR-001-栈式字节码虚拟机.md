# ADR-001：采用栈式字节码虚拟机

- 状态：已接受
- 日期：2026-08-04

华言源代码经过 Lexer、Parser、Resolver 和 Compiler 后生成自有字节码，由栈式 VM 执行。第一版字节码只在内存中使用，不承诺跨版本兼容。

选择栈式 VM 是为了保持实现小、便于反汇编和错误定位，并让语言语义不依赖 Go 的 `eval`、反射或宿主对象。任何优化都必须保持字节码源码映射和错误调用栈。
