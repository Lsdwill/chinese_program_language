# ADR-002：局部槽位与 Upvalue 捕获

- 状态：已接受并实现
- 日期：2026-08-04

Resolver 为参数和局部变量分配槽位。Compiler 为每个捕获名称分配稳定的 Upvalue 索引，并生成局部槽位读写和显式上值读写指令。闭包创建时登记开放 Upvalue；词法作用域退出或函数返回时将绑定值关闭到堆对象，多个闭包可以共享同一绑定单元，关闭后的读写不再依赖已返回的函数帧。

名称解析、编译和 VM 不直接通过运行时名称查找实现局部变量。模块名称保留独立的模块环境和导出表。
