# 华言中文脚本语言开发计划

> 文档状态：设计基线  
> 版本：0.1  
> 日期：2026-08-03  
> 项目代号：华言（暂定）  
> 源文件扩展名：.hua，兼容 .华  
> 解释器命令：huayan

## 1. 文档目的

本文档定义一门独立中文脚本语言从零到可用版本的完整开发计划。

最终用户应能像运行 Python 脚本一样，在终端执行：

~~~bash
huayan hello.hua
huayan 图书馆/主.hua
~~~

华言不是中文关键字到 Python 的文本替换，也不调用 Python eval。华言拥有自己的语法、语义、字节码、虚拟机、模块系统和中文标准库。

Go 只作为第一代编译器和虚拟机的实现语言。发布后的 huayan 是独立可执行程序，用户不需要安装 Go 或 Python。

## 2. 项目愿景

华言的目标是一门：

- 使用中文关键字、中文标准库和中文错误信息的脚本语言；
- 语法规模接近早期 Lua，使用体验接近 Python；
- 支持动态强类型、词法作用域、一等函数、闭包和模块；
- 能够开发命令行工具、数据处理程序和小型业务系统；
- 可以生成单个跨平台解释器程序；
- 语言核心足够小，便于学习、维护和演进。

首个完整应用是命令行图书馆系统。它用于验证华言能否完成真实的数据建模、业务校验、模块组织、文件持久化和错误处理。

## 3. 已确定的设计基线

| 项目 | 决定 |
|---|---|
| 语言类别 | 通用脚本语言 |
| 实现语言 | Go |
| 执行方式 | 源代码编译为自有字节码，由栈式虚拟机执行 |
| 类型系统 | 动态强类型 |
| 作用域 | 词法作用域 |
| 函数 | 一等值，支持递归和闭包 |
| 变量 | 必须显式声明，不允许隐式全局变量 |
| 代码块 | 如果、否则、函数、结束等中文关键字 |
| 语句结束 | 换行；分号可选 |
| 列表下标 | 从 0 开始 |
| 字符串 | UTF-8、不可变，按 Unicode 码点处理 |
| 错误模型 | 可抛出、可捕获的错误对象 |
| 模块模型 | 一个源文件对应一个模块，显式导出 |
| 内存管理 | 第一代运行时使用 Go 垃圾回收 |
| 第一版持久化 | 文件和 JSON |
| 第一版应用 | 单机命令行图书馆系统 |

任何改变这些基线的决定，都应先增加一份架构决策记录，再修改语言规范和实现。

## 4. 范围

### 4.1 第一阶段必须完成

- UTF-8 中文源代码；
- 中文标识符和关键字；
- 整数、小数、布尔、空值和文字；
- 列表、字典和记录；
- 变量、常量和赋值；
- 条件、循环、跳出和继续；
- 函数、递归、闭包和返回；
- 词法作用域与名称解析；
- 模块、导入和显式导出；
- 索引访问和记录字段访问；
- 错误抛出、捕获和调用栈；
- 文件、JSON、时间和控制台标准库；
- 字节码编译器和虚拟机；
- REPL、语法检查和字节码反汇编；
- 中文诊断信息；
- 命令行图书馆系统；
- Linux、Windows、macOS 基本运行能力。

### 4.2 第一阶段明确不做

- 类、继承、接口和复杂面向对象系统；
- 泛型和完整静态类型系统；
- 宏和编译期元编程；
- 协程、线程和异步运行时；
- 包管理器和公共包仓库；
- JIT 或原生机器码；
- Web 框架和图形界面；
- PostgreSQL/MySQL 驱动；
- 自动内存管理器的自主实现；
- 编译器自举；
- 与 Python 对象或 Python 模块的直接互操作。

这些能力不应提前混入第一版，否则会阻塞语言核心和图书馆系统交付。

## 5. 用户体验目标

### 5.1 运行源文件

hello.hua：

~~~text
打印("你好，世界！")
~~~

运行：

~~~bash
huayan hello.hua
~~~

输出：

~~~text
你好，世界！
~~~

### 5.2 交互环境

~~~text
$ huayan
华言 0.1.0
输入“退出”离开

>>> 让 名字 = "小明"
>>> 打印("你好，" + 名字)
你好，小明
>>> 1 + 2
3
~~~

### 5.3 执行图书馆系统

~~~bash
huayan examples/图书馆/主.hua
~~~

预期进入：

~~~text
华言图书馆系统

1. 添加图书
2. 查询图书
3. 登记读者
4. 借阅图书
5. 归还图书
6. 查看借阅记录
0. 退出

请选择：
~~~

## 6. 词法设计

### 6.1 源文件

- 默认编码必须是 UTF-8；
- 接受 UTF-8 BOM，但内部忽略；
- 换行统一为 LF；
- 保存原始字节偏移、行号和 Unicode 字符列；
- 源码读取失败必须作为编译错误报告；
- 默认扩展名为 .hua；
- .华 可以作为别名，但工具链必须优先保证 .hua。

### 6.2 Unicode 规范

- 标识符在比较前执行 NFC 规范化；
- 标识符遵循 Unicode XID_Start 和 XID_Continue；
- 允许汉字、合法 Unicode 字母和下划线；
- 数字不能出现在标识符开头；
- 第一版不允许 Emoji 作为标识符；
- 保留原始拼写用于错误信息；
- 字符串内容不做规范化，必须保持用户原始数据；
- 字符串长度按 Unicode 码点计算，而不是 UTF-8 字节数。

合法标识符示例：

~~~text
图书
图书编号
订单_编号
reader_id
~~~

非法标识符示例：

~~~text
123图书
图书-编号
~~~

### 6.3 关键字

第一版保留以下关键字：

~~~text
模块  导入  从  为  公开
让  常量
函数  返回
如果  否则  结束
当  遍历  于
跳出  继续
真  假  空
且  或  非
尝试  捕获  抛出
记录
~~~

关键字不能作为普通变量名。标准库名称不是关键字，可以被局部变量遮蔽，但格式化和检查工具应给出可读性提示。

### 6.4 注释

支持：

~~~text
// 单行注释

/*
多行注释
*/
~~~

第一版多行注释不嵌套。

### 6.5 标点

解析器内部使用 ASCII 标点。词法分析器在字符串外接受以下全角等价形式：

| 中文标点 | 内部标点 |
|---|---|
| （ | ( |
| ） | ) |
| ［ | [ |
| ］ | ] |
| ｛ | { |
| ｝ | } |
| ， | , |
| ： | : |
| ； | ; |

字符串中的全角标点必须原样保留。

### 6.6 换行和分号

- 普通换行结束当前语句；
- 圆括号、方括号和花括号内部的换行不结束语句；
- 运算符后面的换行不结束表达式；
- 分号可以显式结束语句；
- 格式化工具默认不输出分号。

示例：

~~~text
让 总数 = 10

让 结果 = (
    总数
    + 20
)
~~~

### 6.7 字面量

~~~text
123
-42
3.14
"中文文字"
"换行：\n"
真
假
空
[1, 2, 3]
{"B001": 3, "B002": 5}
记录 { 编号: "B001", 书名: "活着" }
~~~

第一版只使用阿拉伯数字，不把“一百二十三”识别为数字。

## 7. 语义设计

### 7.1 值类型

| 类型 | 说明 | 可变性 |
|---|---|---|
| 空值 | 唯一值空 | 不可变 |
| 布尔 | 真、假 | 不可变 |
| 整数 | 64 位有符号整数 | 不可变 |
| 小数 | IEEE 754 双精度浮点数 | 不可变 |
| 文字 | Unicode 字符串 | 不可变 |
| 列表 | 有序值集合 | 可变 |
| 字典 | 键值映射 | 可变 |
| 记录 | 字符串字段集合，支持点号访问 | 可变 |
| 函数 | 用户函数、闭包或原生函数 | 不可变 |
| 模块 | 导出值的命名空间 | 初始化后只读 |
| 错误 | 错误类别、消息和调用栈 | 不可变 |

### 7.2 数字

- 整数使用 64 位有符号范围；
- 整数溢出产生运行时错误；
- 小数使用 float64；
- 整数与小数混合运算时，整数提升为小数；
- 布尔值不是数字；
- 除以零产生运行时错误；
- 图书编号、读者编号等业务编号应使用文字。

### 7.3 变量和常量

~~~text
让 数量 = 10
数量 = 数量 + 1

常量 最大借阅数 = 5
~~~

规则：

- 让声明可重新赋值的词法绑定；
- 常量声明不可重新赋值；
- 声明时必须提供初始值；
- 给未声明名称赋值是编译错误；
- 同一作用域不允许重复声明同名变量；
- 内层作用域可以遮蔽外层名称；
- 读取初始化完成前的变量是编译错误；
- 顶层变量属于当前模块，不自动成为进程全局变量。

### 7.4 条件和逻辑运算

条件必须是布尔值：

~~~text
如果 图书 != 空
    打印(图书.书名)
结束
~~~

下面代码应报类型错误：

~~~text
如果 图书
    打印(图书.书名)
结束
~~~

逻辑运算符为：

~~~text
且
或
非
~~~

且和或必须短路求值。

### 7.5 运算符

从低到高的建议优先级：

1. 赋值；
2. 或；
3. 且；
4. 相等：==、!=；
5. 比较：<、<=、>、>=；
6. 加减：+、-；
7. 乘除取余：*、/、%；
8. 一元：非、-；
9. 调用、索引和字段访问。

不进行隐式字符串转换：

~~~text
// 错误
"数量：" + 10

// 正确
"数量：" + 转文字(10)
~~~

### 7.6 相等性

- 空值只等于空值；
- 布尔、整数、小数和文字按值比较；
- 整数和小数允许数值比较；
- 列表、字典、记录、函数和模块默认按对象身份比较；
- 后续可提供深度相等标准函数，但不改变 == 的身份语义。

### 7.7 列表

~~~text
让 图书列表 = ["活着", "三体"]
图书列表.追加("围城")
打印(图书列表[0])
~~~

规则：

- 下标从 0 开始；
- 下标必须为整数；
- 越界访问产生运行时错误；
- 第一版不支持负数下标；
- 列表赋值采用引用语义；
- 遍历期间修改列表长度的行为必须报错，避免未定义行为。

### 7.8 字典

~~~text
让 库存 = {
    "B001": 3,
    "B002": 5
}

库存["B001"] = 4
~~~

第一版允许布尔、整数和文字作为字典键。空值、小数和可变对象不能作为键。

### 7.9 记录

~~~text
让 图书 = 记录 {
    编号: "B001",
    书名: "活着",
    作者: "余华",
    借阅人: 空
}

打印(图书.书名)
图书.借阅人 = "R001"
~~~

记录设计规则：

- 字段名必须是标识符或文字；
- 记录保存字段插入顺序；
- 点号访问是字符串字段访问的语法糖；
- 读取不存在字段产生运行时错误；
- 可以通过内置函数检查字段是否存在；
- JSON 对象默认解析为记录还是字典，必须由 JSON API 参数明确决定；
- 第一版没有记录类型声明、方法和继承。

### 7.10 函数和闭包

~~~text
函数 创建计数器()
    让 次数 = 0

    函数 增加()
        次数 = 次数 + 1
        返回 次数
    结束

    返回 增加
结束
~~~

规则：

- 函数是第一等值；
- 参数按值传递；可变对象传递的是对象引用值；
- 参数数量必须严格匹配；
- 没有显式返回时返回空；
- 支持递归和嵌套函数；
- 闭包捕获词法作用域中的绑定单元；
- 第一版不支持默认参数、命名参数和可变参数；
- 最大调用深度由虚拟机配置，超限报告调用栈溢出。

### 7.11 控制流

~~~text
如果 分数 >= 90
    打印("优秀")
否则如果 分数 >= 60
    打印("及格")
否则
    打印("不及格")
结束

当 队列.长度() > 0
    处理(队列.移除首项())
结束

遍历 图书 于 图书列表
    如果 图书.编号 == 目标编号
        返回 图书
    结束
结束
~~~

跳出和继续只能出现在循环内。返回只能出现在函数内。这些限制由名称解析阶段检查。

### 7.12 错误

~~~text
函数 借书(图书)
    如果 图书.借阅人 != 空
        抛出 错误("图书已经借出")
    结束
结束

尝试
    借书(图书)
捕获 原因
    打印("借书失败：" + 原因.消息)
结束
~~~

第一版规则：

- 任何值都不能直接抛出，抛出目标必须是错误对象；
- 错误对象至少包含类别、消息、源码位置和调用栈；
- 捕获接收当前错误对象；
- 捕获后可以继续执行或再次抛出；
- 未捕获错误终止程序并返回非零退出码；
- 第一版不实现 finally，资源接口应提供自动关闭或显式关闭能力。

## 8. 模块系统

### 8.1 模块声明

~~~text
模块 图书馆.服务.借阅
~~~

模块名应与文件相对路径一致。检查工具负责验证不一致问题。

### 8.2 导入

~~~text
导入 标准.文件 为 文件
导入 标准.JSON 为 JSON

从 图书馆.服务.借阅 导入 借书, 还书
~~~

### 8.3 导出

~~~text
公开 函数 借书(图书编号, 读者编号)
    // ...
结束

函数 验证库存(图书)
    // 私有函数
结束
~~~

规则：

- 只有公开声明可以被其他模块导入；
- 模块顶层代码只执行一次；
- 加载完成的模块进入当前进程的模块缓存；
- 相对项目根目录解析用户模块；
- 标准模块使用标准前缀；
- 模块路径不能逃出项目根目录；
- 第一版遇到循环导入直接报告编译错误；
- 导入失败应包含完整导入链。

## 9. 核心语法草案

以下文法用于指导实现，不替代后续正式语言规范。

~~~text
程序         ::= [模块声明] 换行* 声明* 文件结束

模块声明     ::= "模块" 模块路径

声明         ::= 导入声明
               | 公开声明
               | 变量声明
               | 常量声明
               | 函数声明
               | 语句

公开声明     ::= "公开" (变量声明 | 常量声明 | 函数声明)

变量声明     ::= "让" 标识符 "=" 表达式 语句结束
常量声明     ::= "常量" 标识符 "=" 表达式 语句结束

函数声明     ::= "函数" 标识符 "(" [参数列表] ")" 换行
                 声明*
                 "结束" 语句结束

参数列表     ::= 标识符 ("," 标识符)*

语句         ::= 如果语句
               | 当语句
               | 遍历语句
               | 尝试语句
               | 返回语句
               | 跳出语句
               | 继续语句
               | 抛出语句
               | 表达式语句

如果语句     ::= "如果" 表达式 换行
                 声明*
                 ("否则如果" 表达式 换行 声明*)*
                 ["否则" 换行 声明*]
                 "结束" 语句结束

当语句       ::= "当" 表达式 换行
                 声明*
                 "结束" 语句结束

遍历语句     ::= "遍历" 标识符 "于" 表达式 换行
                 声明*
                 "结束" 语句结束

尝试语句     ::= "尝试" 换行
                 声明*
                 "捕获" 标识符 换行
                 声明*
                 "结束" 语句结束

返回语句     ::= "返回" [表达式] 语句结束
跳出语句     ::= "跳出" 语句结束
继续语句     ::= "继续" 语句结束
抛出语句     ::= "抛出" 表达式 语句结束

表达式       ::= 赋值
赋值         ::= 逻辑或 [ "=" 赋值 ]
逻辑或       ::= 逻辑与 ("或" 逻辑与)*
逻辑与       ::= 相等 ("且" 相等)*
相等         ::= 比较 (("==" | "!=") 比较)*
比较         ::= 加减 (("<" | "<=" | ">" | ">=") 加减)*
加减         ::= 乘除 (("+" | "-") 乘除)*
乘除         ::= 一元 (("*" | "/" | "%") 一元)*
一元         ::= ("非" | "-") 一元 | 后缀
后缀         ::= 基本表达式 (调用 | 索引 | 字段访问)*
调用         ::= "(" [实参列表] ")"
索引         ::= "[" 表达式 "]"
字段访问     ::= "." 标识符
~~~

表达式解析建议使用 Pratt Parser，声明和语句使用递归下降解析器。

## 10. 编译器总体架构

~~~text
UTF-8 源文件
    ↓
源码管理器 Source Manager
    ↓
词法分析器 Lexer
    ↓
Token 流
    ↓
语法分析器 Parser
    ↓
抽象语法树 AST
    ↓
名称解析器 Resolver
    ↓
字节码编译器 Compiler
    ↓
函数原型、常量池、字节码 Chunk
    ↓
栈式虚拟机 VM
    ↓
运行时对象、原生函数和标准库
~~~

### 10.1 源码管理器

负责：

- 读取和缓存源文件；
- 给每个文件分配稳定编号；
- 字节偏移到行列位置的映射；
- 提取错误上下文；
- 管理模块路径与物理路径；
- 支持 REPL 中的虚拟源文件。

### 10.2 词法分析器

负责：

- 识别关键字、标识符、字面量和运算符；
- 处理注释和换行；
- 处理全角标点；
- 记录每个 Token 的源码范围；
- 对未闭合字符串、非法转义和非法字符给出可恢复诊断。

词法分析器不能依赖字符串替换中文关键字。

### 10.3 语法分析器

负责：

- 从 Token 构建 AST；
- 实现表达式优先级；
- 报告缺失结束、括号和参数等问题；
- 在语句边界进行错误恢复；
- 尽可能在一次检查中报告多个语法错误。

### 10.4 名称解析器

负责：

- 建立词法作用域栈；
- 区分局部变量、闭包变量和模块变量；
- 给局部变量分配槽位；
- 识别被闭包捕获的变量；
- 检查重复声明和未声明名称；
- 检查返回、跳出、继续的位置是否合法；
- 解析导入和导出名称；
- 为编译器生成符号绑定信息。

### 10.5 字节码编译器

负责：

- 把 AST 转换为栈式字节码；
- 管理常量池；
- 生成条件跳转并回填地址；
- 编译函数原型和闭包捕获描述；
- 保存字节码到源码位置映射；
- 执行安全的常量折叠；
- 在调试模式下生成可读反汇编。

第一版字节码只在内存中使用，不承诺跨版本兼容。缓存文件格式放到后续版本。

## 11. 虚拟机设计

### 11.1 值表示

Go 侧可以先使用带类型标签的 Value 结构表示：

~~~text
空值
布尔
整数
小数
对象引用
~~~

对象引用指向：

~~~text
文字
列表
字典
记录
函数原型
闭包
原生函数
模块
错误
~~~

第一版优先保证语义正确，不提前实现 NaN boxing、指针标记或对象池。

### 11.2 调用栈

每个调用帧至少包含：

- 当前闭包；
- 指令指针；
- 栈基址；
- 模块上下文；
- 错误处理区；
- 调试源码信息。

需要配置最大：

- 操作数栈大小；
- 调用深度；
- 单函数局部变量数；
- 常量池大小；
- 字节码长度。

超限必须产生可理解的错误，不能让 Go 进程直接崩溃。

### 11.3 闭包

闭包必须捕获绑定单元，而不是捕获当时值。被捕获的局部变量离开栈帧后需要提升为堆对象。

必须覆盖以下测试：

- 读取外层变量；
- 修改外层变量；
- 多个闭包共享一个变量；
- 嵌套多层捕获；
- 递归闭包；
- 闭包在创建函数返回后继续工作。

### 11.4 指令集合

第一版建议包含：

~~~text
常量与栈：
  加载常量、加载空、加载真、加载假、弹出、复制栈顶

变量：
  读取局部、写入局部
  读取闭包变量、写入闭包变量、关闭闭包变量
  读取模块变量、写入模块变量

运算：
  加、减、乘、除、取余、取负
  相等、不等、小于、小于等于、大于、大于等于
  逻辑非

控制流：
  跳转、条件假跳转、循环回跳

函数：
  创建闭包、调用、返回

集合：
  创建列表、创建字典、创建记录
  读取索引、写入索引
  读取字段、写入字段

模块：
  导入模块、读取导出

错误：
  建立捕获区、退出捕获区、抛出
~~~

### 11.5 原生函数接口

标准库通过受控的 Go 原生函数接口进入 VM。接口需要：

- 检查参数数量；
- 检查和转换华言值；
- 返回华言值或华言错误；
- 不能直接触碰 VM 私有栈；
- 能够获得当前源码调用位置；
- 对文件句柄等资源提供明确生命周期；
- 捕获 Go panic，转换为内部故障报告，不能暴露宿主崩溃。

### 11.6 内存管理

第一代 VM 使用 Go 垃圾回收管理运行时对象。这不会改变华言的语言语义。

需要避免：

- 把 Go map 的随机遍历顺序泄漏为华言语义；
- 把 Go panic 当作华言异常；
- 把 Go 字节字符串长度当作华言文字长度；
- 直接暴露 Go 对象给用户代码；
- 依赖 Go 反射决定华言类型行为。

未来如果需要可控暂停时间或独立运行时，再设计华言自己的标记清除垃圾回收器。

## 12. 中文诊断系统

错误必须包含：

- 错误类别和编号；
- 中文错误消息；
- 文件名、行号和字符列；
- 源码片段；
- 精确下划线；
- 可行时给出修复建议；
- 运行时错误附带华言调用栈。

示例：

~~~text
名称错误 E2001：变量“图数量”尚未声明
  --> 图书馆/主.hua:12:9

12 | 打印(图数量)
   |      ^^^^^^

建议：是否想使用“图书数量”？
~~~

诊断系统从第一个里程碑开始建设，不能在项目末期补做。

## 13. 标准库计划

### 13.1 顶层内置函数

第一版只保留少量高频内置：

~~~text
打印
输入
长度
类型
转文字
转整数
转小数
范围
错误
断言
~~~

其余能力放入模块，避免全局命名空间膨胀。

### 13.2 标准模块

| 模块 | 第一版能力 |
|---|---|
| 标准.控制台 | 输出、错误输出、读取一行 |
| 标准.文字 | 查找、替换、分割、裁剪、大小写处理 |
| 标准.列表 | 排序、查找、映射、过滤 |
| 标准.字典 | 键、值、条目、包含键 |
| 标准.文件 | 读取文字、写入文字、追加、存在、创建目录、原子写入 |
| 标准.JSON | 解析、序列化、格式化输出 |
| 标准.时间 | 当前时间、日期解析、日期格式化、日期差 |
| 标准.数学 | 绝对值、最小值、最大值、取整 |
| 标准.程序 | 命令行参数、环境信息、退出 |
| 标准.测试 | 断言相等、断言错误、测试注册与运行 |

标准库 API 必须使用中文命名，并为每个公开函数提供：

- 参数说明；
- 返回类型说明；
- 错误条件；
- 至少一个示例；
- 边界测试。

### 13.3 JSON 映射

默认映射：

| JSON | 华言 |
|---|---|
| null | 空 |
| boolean | 布尔 |
| number 无小数部分 | 整数，溢出时小数或错误 |
| number 有小数部分 | 小数 |
| string | 文字 |
| array | 列表 |
| object | 记录 |

序列化函数必须检测循环引用并报告错误。

## 14. 命令行工具

核心用法：

~~~bash
huayan 文件.hua
huayan 文件.hua -- 参数1 参数2
huayan -c '打印("你好")'
huayan
~~~

辅助命令建议同时支持英文和中文别名：

~~~bash
huayan check 文件.hua
huayan 检查 文件.hua

huayan fmt 文件.hua
huayan 格式化 文件.hua

huayan dis 文件.hua
huayan 字节码 文件.hua

huayan test tests/
huayan 测试 tests/
~~~

建议退出码：

| 退出码 | 含义 |
|---|---|
| 0 | 成功 |
| 1 | 未捕获运行时错误 |
| 2 | 词法、语法、名称或编译错误 |
| 3 | 模块或文件加载错误 |
| 64 | 命令行参数错误 |
| 70 | 解释器内部错误 |

Linux 和 macOS 支持：

~~~text
#!/usr/bin/env huayan
~~~

Windows 通过 huayan.exe 执行。

## 15. 建议仓库结构

~~~text
.
├── README.md
├── LICENSE
├── go.mod
├── cmd/
│   └── huayan/
│       └── main.go
├── internal/
│   ├── source/
│   ├── token/
│   ├── lexer/
│   ├── ast/
│   ├── parser/
│   ├── resolver/
│   ├── bytecode/
│   ├── compiler/
│   ├── value/
│   ├── object/
│   ├── vm/
│   ├── module/
│   ├── native/
│   ├── diagnostic/
│   └── repl/
├── stdlib/
├── docs/
│   ├── spec/
│   │   ├── 词法规范.md
│   │   ├── 语法规范.md
│   │   ├── 语义规范.md
│   │   ├── 模块规范.md
│   │   └── 标准库规范.md
│   ├── decisions/
│   └── tutorials/
├── tests/
│   ├── conformance/
│   ├── errors/
│   ├── integration/
│   └── fuzz/
├── examples/
│   ├── 你好世界.hua
│   ├── 斐波那契.hua
│   ├── 猜数字.hua
│   └── 图书馆/
└── tools/
    ├── formatter/
    └── syntax/
~~~

internal 目录中的 Go 名称建议使用英文，避免与 Go 工具和第三方生态产生兼容问题。华言源代码、标准库 API、文档、示例和诊断保持中文。

## 16. 开发里程碑

工期按单人全职、已有 Go 基础估算。实际时间会受到测试深度、语言设计变更和跨平台要求影响。

### M0：规范冻结与工程骨架

预计：3～5 人日。

交付：

- README 和项目目标；
- Go 模块和命令行骨架；
- 词法、语法、语义基线文档；
- Token、SourceSpan、Diagnostic 核心数据结构；
- 自动化测试入口；
- 架构决策记录模板。

验收：

- huayan --version 可运行；
- go test ./... 通过；
- 关键语义不存在未决阻塞项。

### M1：源码管理与词法分析

预计：5～8 人日。

交付：

- UTF-8 源码读取；
- Unicode 标识符；
- 关键字、字面量、操作符和注释；
- 换行和全角标点处理；
- Token 调试输出；
- 中文词法错误。

验收：

- hello.hua 能正确输出 Token；
- 中文标识符和全角标点测试通过；
- 未闭合字符串和非法字符有精确位置；
- 词法 Fuzz 测试不能导致崩溃。

### M2：AST 与语法分析

预计：8～12 人日。

交付：

- AST 节点；
- Pratt 表达式解析器；
- 声明、函数、条件、循环和错误语句；
- 列表、字典和记录字面量；
- 错误恢复；
- AST 调试打印。

验收：

- 语言语法示例全部可解析；
- 运算符优先级测试通过；
- 缺少结束、括号和参数时能继续报告后续错误；
- 解析器 Fuzz 测试不能导致崩溃或死循环。

### M3：名称解析、字节码与基础 VM

预计：10～15 人日。

交付：

- 词法作用域和符号表；
- 局部变量槽位；
- 常量池和字节码块；
- 基础操作数栈；
- 数字、文字、布尔和空值；
- 变量、算术、比较和条件跳转；
- 字节码反汇编。

验收：

- 算术、变量和条件程序可以执行；
- 未声明变量在执行前被发现；
- 字节码保留源码位置；
- VM 遇到损坏字节码时安全失败。

### M4：函数、调用栈与闭包

预计：10～15 人日。

交付：

- 函数原型；
- 调用帧；
- 参数和返回值；
- 递归；
- 闭包变量捕获与提升；
- 华言调用栈。

验收：

- 斐波那契程序通过；
- 计数器闭包程序通过；
- 多层闭包共享修改测试通过；
- 调用栈溢出产生华言错误而不是 Go 崩溃。

### M5：集合、记录和异常

预计：10～15 人日。

交付：

- 列表、字典和记录对象；
- 索引与字段操作；
- 遍历；
- 错误对象；
- 尝试、捕获和抛出；
- 循环引用检测基础设施。

验收：

- 通讯录示例可以运行；
- 越界、字段不存在和键类型错误有中文诊断；
- 未捕获错误显示完整华言调用栈；
- JSON 所需的值模型稳定。

### M6：模块与应用标准库

预计：12～18 人日。

交付：

- 模块解析、缓存、公开与导入；
- 控制台、文件、JSON、时间和程序模块；
- 命令行参数；
- 跨模块错误栈；
- 原子文件写入。

验收：

- 多文件程序可以运行；
- 循环导入有明确错误；
- JSON 数据可读取、修改和安全写回；
- Linux、Windows、macOS 路径测试通过。

### M7：REPL、检查器和质量加固

预计：8～12 人日。

交付：

- 多行 REPL；
- check、dis 和 test 命令；
- 基础格式化器；
- 一致性测试套件；
- 性能基准；
- 安装与发布脚本。

验收：

- REPL 支持多行函数和代码块；
- 格式化器重复运行结果不变；
- 所有一致性测试通过；
- 构建产物不依赖 Go 安装环境。

### M8：图书馆系统

预计：10～15 人日。

交付：

- 图书、读者和借阅记录模型；
- 添加、查询、借阅和归还业务；
- JSON 持久化；
- 命令行菜单；
- 业务测试；
- 示例数据和使用文档。

验收：

~~~bash
huayan examples/图书馆/主.hua
~~~

可以完成完整业务流程，退出后重新启动数据仍然存在。

### 总体工作量

第一条可用链路预计约 76～110 人日，约为单人全职 4～6 个月。业余开发应按 6～12 个月规划。

该估算不包括 IDE、包管理器、数据库、HTTP、JIT、静态类型和自举编译器。

## 17. 图书馆系统设计

### 17.1 第一版边界

第一版是单机、单进程、命令行系统：

- 数据保存在本地 JSON 文件；
- 不支持多个进程同时写入；
- 不提供网络服务；
- 不实现用户登录和权限管理；
- 不依赖数据库；
- 重点验证语言和标准库是否足够开发业务程序。

如果后续需要多人同时使用，应增加 SQLite 或服务端数据库、事务、鉴权和 HTTP 层，这属于第二阶段平台能力。

### 17.2 数据实体

图书：

~~~text
记录 {
    编号: "B001",
    书名: "活着",
    作者: "余华",
    分类: "文学",
    出版年份: 1993,
    状态: "可借"
}
~~~

读者：

~~~text
记录 {
    编号: "R001",
    姓名: "张三",
    联系方式: "13800000000",
    状态: "正常"
}
~~~

借阅记录：

~~~text
记录 {
    编号: "L001",
    图书编号: "B001",
    读者编号: "R001",
    借出时间: "2026-08-03T10:00:00+08:00",
    应还时间: "2026-09-03T10:00:00+08:00",
    归还时间: 空,
    状态: "借出"
}
~~~

### 17.3 核心业务规则

- 图书编号必须唯一；
- 读者编号必须唯一；
- 只有状态为可借的图书可以借出；
- 只有状态为正常的读者可以借书；
- 每位读者最多同时借阅 5 本；
- 借书时同时创建借阅记录并修改图书状态；
- 归还时必须找到对应的有效借阅记录；
- 归还后修改图书状态和借阅记录；
- 删除图书前不能存在未归还记录；
- 所有输入必须验证；
- 数据写入使用临时文件加原子替换，避免中断导致文件损坏。

### 17.4 模块划分

~~~text
examples/图书馆/
├── 主.hua
├── 配置.hua
├── 模型/
│   ├── 图书.hua
│   ├── 读者.hua
│   └── 借阅记录.hua
├── 仓库/
│   ├── 图书仓库.hua
│   ├── 读者仓库.hua
│   └── 借阅仓库.hua
├── 服务/
│   ├── 图书服务.hua
│   ├── 读者服务.hua
│   └── 借阅服务.hua
├── 界面/
│   └── 菜单.hua
├── 数据/
│   ├── 图书.json
│   ├── 读者.json
│   └── 借阅记录.json
└── 测试/
    ├── 图书服务测试.hua
    └── 借阅服务测试.hua
~~~

### 17.5 代表性代码

~~~text
模块 图书馆.服务.借阅

导入 标准.时间 为 时间

公开 函数 查找图书(图书列表, 编号)
    遍历 图书 于 图书列表
        如果 图书.编号 == 编号
            返回 图书
        结束
    结束

    返回 空
结束

公开 函数 借书(图书列表, 借阅列表, 图书编号, 读者编号)
    让 图书 = 查找图书(图书列表, 图书编号)

    如果 图书 == 空
        抛出 错误("图书不存在：" + 图书编号)
    结束

    如果 图书.状态 != "可借"
        抛出 错误("图书当前不可借阅")
    结束

    图书.状态 = "借出"

    让 借阅 = 记录 {
        图书编号: 图书编号,
        读者编号: 读者编号,
        借出时间: 时间.现在(),
        归还时间: 空,
        状态: "借出"
    }

    借阅列表.追加(借阅)
    返回 借阅
结束
~~~

### 17.6 图书馆系统验收场景

1. 添加图书后可以立即查询；
2. 退出并重新运行后，新增图书仍然存在；
3. 重复图书编号被拒绝；
4. 登记读者后可以借书；
5. 不存在的读者不能借书；
6. 同一本书不能同时借给两位读者；
7. 借满 5 本后不能继续借阅；
8. 归还后图书恢复可借状态；
9. 重复归还被拒绝；
10. JSON 文件损坏时给出错误，不覆盖原文件；
11. 所有业务错误由华言代码捕获并显示；
12. 未预期错误显示华言调用栈，不出现 Go panic。

## 18. 测试策略

### 18.1 Go 单元测试

覆盖：

- 源码位置和 Unicode 列计算；
- Lexer 每类 Token；
- Parser 每类 AST；
- Resolver 作用域和捕获；
- Compiler 每类指令；
- VM 算术、跳转、调用和错误展开；
- 各标准库模块。

### 18.2 语言一致性测试

每个测试由华言源码、预期标准输出、预期错误输出和退出码组成。

分类：

~~~text
词法
语法
作用域
数字
文字
列表
字典
记录
函数
闭包
模块
错误
标准库
命令行
~~~

一致性测试是语言行为的事实基线。修改测试预期必须同步修改规范或附带架构决策记录。

### 18.3 Golden 测试

用于：

- Token 序列；
- AST 打印；
- 中文诊断；
- 字节码反汇编；
- 格式化结果。

不要对不影响用户语义的内部地址和对象编号做 Golden 固定。

### 18.4 Fuzz 测试

重点目标：

- 随机 UTF-8 和非法字节；
- 极深括号和代码块；
- 未闭合字符串与注释；
- 随机 Token 流；
- 损坏字节码；
- JSON 循环和大嵌套；
- 模块导入路径。

安全目标是不能崩溃、死循环或无限分配。

### 18.5 集成测试

- 构建 huayan 后运行真实 .hua 文件；
- 测试命令行参数和退出码；
- 测试跨模块调用栈；
- 测试文件和 JSON；
- 测试图书馆完整流程；
- 在 Linux、Windows、macOS CI 中运行。

### 18.6 质量门槛

每次合并前：

~~~bash
go test ./...
go test -race ./...
go vet ./...
~~~

并要求：

- 所有语言一致性测试通过；
- 格式化检查通过；
- 核心模块测试覆盖率目标不低于 85%；
- 不存在未说明的语言行为变化；
- 不提交会暴露 Go panic 的用户输入路径。

## 19. 文档计划

第一版发布前必须具备：

- 五分钟入门；
- 安装说明；
- 语言导览；
- 正式词法规范；
- 正式语法规范；
- 正式语义规范；
- 标准库 API；
- 错误信息说明；
- 模块组织指南；
- 图书馆系统教程；
- 从 Python/Lua 到华言的差异说明；
- 版本兼容政策。

规范使用“必须、应该、可以”区分强制要求、推荐行为和可选行为。

## 20. 工程规则

- 手写 Lexer、Parser 和字节码编译器；
- 核心层不依赖大型解析框架；
- AST、Resolver、Compiler、VM 不互相绕层调用；
- 用户可见行为必须先有规范和测试；
- 不把 Go 的 map 顺序、panic、反射语义泄漏给华言；
- SourceSpan 从 Lexer 一直传递到运行时错误；
- 每个新语法特性至少增加正常、边界、错误三个测试；
- 不在同一版本同时引入多个大型语义系统；
- 任何不兼容语法变化必须更新版本和迁移说明；
- 优先完成垂直功能链路，再扩展语法数量。

## 21. 风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 语言范围不断扩大 | 无法交付可用版本 | 严守第一版不做清单 |
| 语法频繁修改 | Parser、文档和示例反复重写 | M0 冻结基线，变化使用决策记录 |
| Unicode 位置计算错误 | 中文错误提示错位 | 同时保存字节偏移和字符列，专项测试 |
| 闭包实现错误 | 隐蔽数据错误 | 捕获模型先写规范，增加共享变量测试 |
| Go 行为泄漏 | 华言语义不稳定 | 自定义 Value/Object 层，禁止直接暴露宿主对象 |
| 模块循环 | 初始化状态不确定 | 第一版明确禁止循环导入 |
| JSON 写入损坏 | 图书馆数据丢失 | 临时文件、刷新、原子替换和备份 |
| VM 崩溃 | 用户程序导致解释器退出 | 字节码验证、边界检查、panic 隔离 |
| 性能过早优化 | 增加复杂度并拖慢开发 | 第一版使用清晰值模型，建立基准后再优化 |
| 标准库 API 不一致 | 中文使用体验混乱 | 制定命名规范和 API 审查 |
| 业务系统反向污染语言 | 出现图书馆专用语法 | 业务只使用通用记录、模块和标准库 |

## 22. 版本路线

### v0.1.0：可运行语言核心

- 基本类型；
- 变量和控制流；
- 函数和闭包；
- 字节码 VM；
- 中文错误；
- REPL；
- 单文件示例。

### v0.2.0：应用语言

- 列表、字典和记录；
- 模块；
- 异常；
- 文件、JSON 和时间；
- 检查、反汇编和测试命令。

### v0.3.0：图书馆系统版本

- 完整命令行图书馆系统；
- 华言测试标准库；
- 格式化工具；
- 三平台构建；
- 完整入门和标准库文档。

### v0.4.0 以后

根据实际需求选择：

- SQLite；
- HTTP 客户端和服务器；
- 包管理器；
- LSP 和 VS Code 插件；
- 类型标注与可选静态检查；
- 协程；
- 持久化字节码缓存；
- 编译器部分自举；
- 自有垃圾回收器或更独立的运行时。

## 23. 第一批可执行任务

按顺序开始：

1. 建立 Go 模块和 cmd/huayan 入口；
2. 实现 huayan --version；
3. 定义 SourceFile、SourceSpan 和 Diagnostic；
4. 定义 TokenKind 和 Token；
5. 实现 UTF-8 Lexer；
6. 为中文标识符、全角标点和错误位置编写测试；
7. 定义第一版 AST；
8. 实现 Pratt 表达式解析器；
9. 实现声明和代码块解析；
10. 实现名称解析器；
11. 定义字节码 Chunk 和反汇编器；
12. 实现数字、打印和返回指令；
13. 运行第一个 hello.hua；
14. 再逐步加入变量、条件、循环和函数。

第一个阶段性演示只要求：

~~~bash
huayan examples/你好世界.hua
~~~

输出：

~~~text
你好，世界！
~~~

但这条链路必须已经通过华言自己的 Lexer、Parser、Compiler 和 VM，而不是由 Go/Python 执行源码语义。

## 24. 项目完成定义

第一阶段项目只有满足以下条件才算完成：

- huayan 是独立可执行文件；
- 不安装 Python 或 Go 也能执行华言程序；
- 能直接运行 huayan 文件.hua；
- 中文关键字、标识符、标准库和错误信息稳定；
- 源文件经过自有词法、语法、名称解析和字节码编译；
- 虚拟机支持函数、闭包、模块、集合和异常；
- 标准库支持控制台、文件、JSON 和时间；
- 图书馆系统完全使用华言编写；
- 图书馆系统可以新增、查询、借阅、归还并持久化数据；
- 所有一致性测试和集成测试通过；
- Linux、Windows、macOS 至少各有一个可运行发布包；
- 文档足以让新用户在 15 分钟内运行第一个华言程序。

达到这些条件后，华言才从“语法实验”进入“可开发小型真实应用的中文脚本语言”阶段。
