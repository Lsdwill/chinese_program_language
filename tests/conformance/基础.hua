导入 标准.测试 为 测试

函数 测试算术()
    测试.断言相等(1 + 2 * 3, 7)
结束

函数 测试闭包()
    函数 创建()
        让 次数 = 0
        函数 增加()
            次数 = 次数 + 1
            返回 次数
        结束
        返回 增加
    结束
    让 增加 = 创建()
    测试.断言相等(增加(), 1)
    测试.断言相等(增加(), 2)
结束

测试.测试("算术", 测试算术)
测试.测试("闭包", 测试闭包)
测试.运行()
