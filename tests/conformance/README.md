# 语言一致性案例

每个 `.hua` 文件都是一个独立案例，并使用同名文件记录期望结果：

- `.out`：标准输出，按字节比较；
- `.err`：标准错误，内容为 `<空>` 表示必须为空；
- `.exit`：十进制退出码。

运行全部案例：

```bash
scripts/check-conformance.sh ./huayan
```

案例使用发布二进制执行，不依赖 Python 或其他解释器。
